# Team dashboard runs on Spring Boot 3+ (Java 17, jakarta.servlet).
# Large uploads need no container tuning: the chunked upload endpoint streams
# instead of buffering through Spring multipart.
spring.servlet.multipart.enabled=false
server.port=8080
