// Imported by Countdown.client.js: served under the same content hash (ADR 0022).
import { mark } from "../shared/marks.js";

/** @param {number} left */
export const faceText = (left) => String(left);

export const graphMark = mark("graph");
