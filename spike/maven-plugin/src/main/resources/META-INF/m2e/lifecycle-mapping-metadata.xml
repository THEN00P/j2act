<?xml version="1.0" encoding="UTF-8"?>
<!-- Eclipse (m2e) runs the bundle goal on incremental builds too; the goal itself skips when no
     frontend file changed, and refreshes its output so Eclipse sees it. -->
<lifecycleMappingMetadata>
  <pluginExecutions>
    <pluginExecution>
      <pluginExecutionFilter>
        <goals>
          <goal>bundle</goal>
        </goals>
      </pluginExecutionFilter>
      <action>
        <execute>
          <runOnIncremental>true</runOnIncremental>
          <runOnConfiguration>false</runOnConfiguration>
        </execute>
      </action>
    </pluginExecution>
  </pluginExecutions>
</lifecycleMappingMetadata>
