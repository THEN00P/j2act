// Generated by j2act-codegen. Do not edit; regenerate with:
//   ./mvnw -pl j2act-codegen compile exec:java
// Data: @vscode/web-custom-data (MIT), @mdn/browser-compat-data (CC0).
// API shape follows j2html (Apache-2.0). See NOTICE.
package j2act.html;

import j2act.DomContent;
import j2act.html.tags.ATag;
import j2act.html.tags.AbbrTag;
import j2act.html.tags.AddressTag;
import j2act.html.tags.AreaTag;
import j2act.html.tags.ArticleTag;
import j2act.html.tags.AsideTag;
import j2act.html.tags.AudioTag;
import j2act.html.tags.BTag;
import j2act.html.tags.BaseTag;
import j2act.html.tags.BdiTag;
import j2act.html.tags.BdoTag;
import j2act.html.tags.BlockquoteTag;
import j2act.html.tags.BodyTag;
import j2act.html.tags.BrTag;
import j2act.html.tags.ButtonTag;
import j2act.html.tags.CanvasTag;
import j2act.html.tags.CaptionTag;
import j2act.html.tags.CiteTag;
import j2act.html.tags.CodeTag;
import j2act.html.tags.ColTag;
import j2act.html.tags.ColgroupTag;
import j2act.html.tags.DataTag;
import j2act.html.tags.DatalistTag;
import j2act.html.tags.DdTag;
import j2act.html.tags.DelTag;
import j2act.html.tags.DetailsTag;
import j2act.html.tags.DfnTag;
import j2act.html.tags.DialogTag;
import j2act.html.tags.DivTag;
import j2act.html.tags.DlTag;
import j2act.html.tags.DtTag;
import j2act.html.tags.EmTag;
import j2act.html.tags.EmbedTag;
import j2act.html.tags.FencedframeTag;
import j2act.html.tags.FieldsetTag;
import j2act.html.tags.FigcaptionTag;
import j2act.html.tags.FigureTag;
import j2act.html.tags.FooterTag;
import j2act.html.tags.FormTag;
import j2act.html.tags.H1Tag;
import j2act.html.tags.H2Tag;
import j2act.html.tags.H3Tag;
import j2act.html.tags.H4Tag;
import j2act.html.tags.H5Tag;
import j2act.html.tags.H6Tag;
import j2act.html.tags.HeadTag;
import j2act.html.tags.HeaderTag;
import j2act.html.tags.HgroupTag;
import j2act.html.tags.HrTag;
import j2act.html.tags.HtmlTag;
import j2act.html.tags.ITag;
import j2act.html.tags.IframeTag;
import j2act.html.tags.ImgTag;
import j2act.html.tags.InputTag;
import j2act.html.tags.InsTag;
import j2act.html.tags.KbdTag;
import j2act.html.tags.LabelTag;
import j2act.html.tags.LegendTag;
import j2act.html.tags.LiTag;
import j2act.html.tags.LinkTag;
import j2act.html.tags.MainTag;
import j2act.html.tags.MapTag;
import j2act.html.tags.MarkTag;
import j2act.html.tags.MenuTag;
import j2act.html.tags.MetaTag;
import j2act.html.tags.MeterTag;
import j2act.html.tags.NavTag;
import j2act.html.tags.NoscriptTag;
import j2act.html.tags.ObjectTag;
import j2act.html.tags.OlTag;
import j2act.html.tags.OptgroupTag;
import j2act.html.tags.OptionTag;
import j2act.html.tags.OutputTag;
import j2act.html.tags.PTag;
import j2act.html.tags.ParamTag;
import j2act.html.tags.PictureTag;
import j2act.html.tags.PreTag;
import j2act.html.tags.ProgressTag;
import j2act.html.tags.QTag;
import j2act.html.tags.RbTag;
import j2act.html.tags.RpTag;
import j2act.html.tags.RtTag;
import j2act.html.tags.RubyTag;
import j2act.html.tags.STag;
import j2act.html.tags.SampTag;
import j2act.html.tags.ScriptTag;
import j2act.html.tags.SearchTag;
import j2act.html.tags.SectionTag;
import j2act.html.tags.SelectTag;
import j2act.html.tags.SelectedcontentTag;
import j2act.html.tags.SlotTag;
import j2act.html.tags.SmallTag;
import j2act.html.tags.SourceTag;
import j2act.html.tags.SpanTag;
import j2act.html.tags.StrongTag;
import j2act.html.tags.StyleTag;
import j2act.html.tags.SubTag;
import j2act.html.tags.SummaryTag;
import j2act.html.tags.SupTag;
import j2act.html.tags.TableTag;
import j2act.html.tags.TbodyTag;
import j2act.html.tags.TdTag;
import j2act.html.tags.TemplateTag;
import j2act.html.tags.TextareaTag;
import j2act.html.tags.TfootTag;
import j2act.html.tags.ThTag;
import j2act.html.tags.TheadTag;
import j2act.html.tags.TimeTag;
import j2act.html.tags.TitleTag;
import j2act.html.tags.TrTag;
import j2act.html.tags.TrackTag;
import j2act.html.tags.UTag;
import j2act.html.tags.UlTag;
import j2act.html.tags.VarTag;
import j2act.html.tags.VideoTag;
import j2act.html.tags.WbrTag;

/**
 * Static tag factories: <code>import static j2act.html.TagCreator.*</code>. Params are children
 * only; attributes are with* builders on the returned tag (ADR 0005). Helpers such as
 * <code>each</code>, <code>iff</code> and <code>join</code> come from TagHelpers.
 */
public final class TagCreator extends TagHelpers {

  private TagCreator() {
  }

  /**
   * Creates a <code>&lt;a&gt;</code> element.
   *
   * <p>If the a element has an href attribute, then it represents a hyperlink (a hypertext anchor)
   * labeled by its contents.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/a">MDN:
   * &lt;a&gt;</a>
   */
  public static ATag a() {
    return new ATag();
  }

  /**
   * Creates a <code>&lt;a&gt;</code> element.
   *
   * <p>If the a element has an href attribute, then it represents a hyperlink (a hypertext anchor)
   * labeled by its contents.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/a">MDN:
   * &lt;a&gt;</a>
   */
  public static ATag a(String text) {
    return new ATag().withText(text);
  }

  /**
   * Creates a <code>&lt;a&gt;</code> element.
   *
   * <p>If the a element has an href attribute, then it represents a hyperlink (a hypertext anchor)
   * labeled by its contents.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/a">MDN:
   * &lt;a&gt;</a>
   */
  public static ATag a(DomContent... children) {
    return new ATag().with(children);
  }

  /**
   * Creates a <code>&lt;abbr&gt;</code> element.
   *
   * <p>The abbr element represents an abbreviation or acronym, optionally with its expansion.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/abbr">MDN:
   * &lt;abbr&gt;</a>
   */
  public static AbbrTag abbr() {
    return new AbbrTag();
  }

  /**
   * Creates a <code>&lt;abbr&gt;</code> element.
   *
   * <p>The abbr element represents an abbreviation or acronym, optionally with its expansion.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/abbr">MDN:
   * &lt;abbr&gt;</a>
   */
  public static AbbrTag abbr(String text) {
    return new AbbrTag().withText(text);
  }

  /**
   * Creates a <code>&lt;abbr&gt;</code> element.
   *
   * <p>The abbr element represents an abbreviation or acronym, optionally with its expansion.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/abbr">MDN:
   * &lt;abbr&gt;</a>
   */
  public static AbbrTag abbr(DomContent... children) {
    return new AbbrTag().with(children);
  }

  /**
   * Creates a <code>&lt;address&gt;</code> element.
   *
   * <p>The address element represents the contact information for its nearest article or body
   * element ancestor.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/address">MDN:
   * &lt;address&gt;</a>
   */
  public static AddressTag address() {
    return new AddressTag();
  }

  /**
   * Creates a <code>&lt;address&gt;</code> element.
   *
   * <p>The address element represents the contact information for its nearest article or body
   * element ancestor.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/address">MDN:
   * &lt;address&gt;</a>
   */
  public static AddressTag address(String text) {
    return new AddressTag().withText(text);
  }

  /**
   * Creates a <code>&lt;address&gt;</code> element.
   *
   * <p>The address element represents the contact information for its nearest article or body
   * element ancestor.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/address">MDN:
   * &lt;address&gt;</a>
   */
  public static AddressTag address(DomContent... children) {
    return new AddressTag().with(children);
  }

  /**
   * Creates a <code>&lt;area&gt;</code> element.
   *
   * <p>The area element represents either a hyperlink with some text and a corresponding area on an
   * image map, or a dead area on an image map.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/area">MDN:
   * &lt;area&gt;</a>
   */
  public static AreaTag area() {
    return new AreaTag();
  }

  /**
   * Creates a <code>&lt;article&gt;</code> element.
   *
   * <p>The article element represents a complete, or self-contained, composition in a document,
   * page, application, or site and that is, in principle, independently distributable or reusable,
   * e.g.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/article">MDN:
   * &lt;article&gt;</a>
   */
  public static ArticleTag article() {
    return new ArticleTag();
  }

  /**
   * Creates a <code>&lt;article&gt;</code> element.
   *
   * <p>The article element represents a complete, or self-contained, composition in a document,
   * page, application, or site and that is, in principle, independently distributable or reusable,
   * e.g.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/article">MDN:
   * &lt;article&gt;</a>
   */
  public static ArticleTag article(String text) {
    return new ArticleTag().withText(text);
  }

  /**
   * Creates a <code>&lt;article&gt;</code> element.
   *
   * <p>The article element represents a complete, or self-contained, composition in a document,
   * page, application, or site and that is, in principle, independently distributable or reusable,
   * e.g.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/article">MDN:
   * &lt;article&gt;</a>
   */
  public static ArticleTag article(DomContent... children) {
    return new ArticleTag().with(children);
  }

  /**
   * Creates a <code>&lt;aside&gt;</code> element.
   *
   * <p>The aside element represents a section of a page that consists of content that is
   * tangentially related to the content around the aside element, and which could be considered
   * separate from that content.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/aside">MDN:
   * &lt;aside&gt;</a>
   */
  public static AsideTag aside() {
    return new AsideTag();
  }

  /**
   * Creates a <code>&lt;aside&gt;</code> element.
   *
   * <p>The aside element represents a section of a page that consists of content that is
   * tangentially related to the content around the aside element, and which could be considered
   * separate from that content.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/aside">MDN:
   * &lt;aside&gt;</a>
   */
  public static AsideTag aside(String text) {
    return new AsideTag().withText(text);
  }

  /**
   * Creates a <code>&lt;aside&gt;</code> element.
   *
   * <p>The aside element represents a section of a page that consists of content that is
   * tangentially related to the content around the aside element, and which could be considered
   * separate from that content.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/aside">MDN:
   * &lt;aside&gt;</a>
   */
  public static AsideTag aside(DomContent... children) {
    return new AsideTag().with(children);
  }

  /**
   * Creates a <code>&lt;audio&gt;</code> element.
   *
   * <p>An audio element represents a sound or audio stream.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/audio">MDN:
   * &lt;audio&gt;</a>
   */
  public static AudioTag audio() {
    return new AudioTag();
  }

  /**
   * Creates a <code>&lt;audio&gt;</code> element.
   *
   * <p>An audio element represents a sound or audio stream.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/audio">MDN:
   * &lt;audio&gt;</a>
   */
  public static AudioTag audio(String text) {
    return new AudioTag().withText(text);
  }

  /**
   * Creates a <code>&lt;audio&gt;</code> element.
   *
   * <p>An audio element represents a sound or audio stream.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/audio">MDN:
   * &lt;audio&gt;</a>
   */
  public static AudioTag audio(DomContent... children) {
    return new AudioTag().with(children);
  }

  /**
   * Creates a <code>&lt;b&gt;</code> element.
   *
   * <p>The b element represents a span of text to which attention is being drawn for utilitarian
   * purposes without conveying any extra importance and with no implication of an alternate voice or
   * mood, such as key words in a document abstract, product names in a review, actionable words in
   * interactive text-driven software, or an article lede.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/b">MDN:
   * &lt;b&gt;</a>
   */
  public static BTag b() {
    return new BTag();
  }

  /**
   * Creates a <code>&lt;b&gt;</code> element.
   *
   * <p>The b element represents a span of text to which attention is being drawn for utilitarian
   * purposes without conveying any extra importance and with no implication of an alternate voice or
   * mood, such as key words in a document abstract, product names in a review, actionable words in
   * interactive text-driven software, or an article lede.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/b">MDN:
   * &lt;b&gt;</a>
   */
  public static BTag b(String text) {
    return new BTag().withText(text);
  }

  /**
   * Creates a <code>&lt;b&gt;</code> element.
   *
   * <p>The b element represents a span of text to which attention is being drawn for utilitarian
   * purposes without conveying any extra importance and with no implication of an alternate voice or
   * mood, such as key words in a document abstract, product names in a review, actionable words in
   * interactive text-driven software, or an article lede.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/b">MDN:
   * &lt;b&gt;</a>
   */
  public static BTag b(DomContent... children) {
    return new BTag().with(children);
  }

  /**
   * Creates a <code>&lt;base&gt;</code> element.
   *
   * <p>The base element allows authors to specify the document base URL for the purposes of
   * resolving relative URLs, and the name of the default browsing context for the purposes of
   * following hyperlinks.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/base">MDN:
   * &lt;base&gt;</a>
   */
  public static BaseTag base() {
    return new BaseTag();
  }

  /**
   * Creates a <code>&lt;bdi&gt;</code> element.
   *
   * <p>The bdi element represents a span of text that is to be isolated from its surroundings for
   * the purposes of bidirectional text formatting.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/bdi">MDN:
   * &lt;bdi&gt;</a>
   */
  public static BdiTag bdi() {
    return new BdiTag();
  }

  /**
   * Creates a <code>&lt;bdi&gt;</code> element.
   *
   * <p>The bdi element represents a span of text that is to be isolated from its surroundings for
   * the purposes of bidirectional text formatting.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/bdi">MDN:
   * &lt;bdi&gt;</a>
   */
  public static BdiTag bdi(String text) {
    return new BdiTag().withText(text);
  }

  /**
   * Creates a <code>&lt;bdi&gt;</code> element.
   *
   * <p>The bdi element represents a span of text that is to be isolated from its surroundings for
   * the purposes of bidirectional text formatting.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/bdi">MDN:
   * &lt;bdi&gt;</a>
   */
  public static BdiTag bdi(DomContent... children) {
    return new BdiTag().with(children);
  }

  /**
   * Creates a <code>&lt;bdo&gt;</code> element.
   *
   * <p>The bdo element represents explicit text directionality formatting control for its children.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/bdo">MDN:
   * &lt;bdo&gt;</a>
   */
  public static BdoTag bdo() {
    return new BdoTag();
  }

  /**
   * Creates a <code>&lt;bdo&gt;</code> element.
   *
   * <p>The bdo element represents explicit text directionality formatting control for its children.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/bdo">MDN:
   * &lt;bdo&gt;</a>
   */
  public static BdoTag bdo(String text) {
    return new BdoTag().withText(text);
  }

  /**
   * Creates a <code>&lt;bdo&gt;</code> element.
   *
   * <p>The bdo element represents explicit text directionality formatting control for its children.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/bdo">MDN:
   * &lt;bdo&gt;</a>
   */
  public static BdoTag bdo(DomContent... children) {
    return new BdoTag().with(children);
  }

  /**
   * Creates a <code>&lt;blockquote&gt;</code> element.
   *
   * <p>The blockquote element represents content that is quoted from another source, optionally with
   * a citation which must be within a footer or cite element, and optionally with in-line changes
   * such as annotations and abbreviations.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/blockquote">MDN:
   * &lt;blockquote&gt;</a>
   */
  public static BlockquoteTag blockquote() {
    return new BlockquoteTag();
  }

  /**
   * Creates a <code>&lt;blockquote&gt;</code> element.
   *
   * <p>The blockquote element represents content that is quoted from another source, optionally with
   * a citation which must be within a footer or cite element, and optionally with in-line changes
   * such as annotations and abbreviations.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/blockquote">MDN:
   * &lt;blockquote&gt;</a>
   */
  public static BlockquoteTag blockquote(String text) {
    return new BlockquoteTag().withText(text);
  }

  /**
   * Creates a <code>&lt;blockquote&gt;</code> element.
   *
   * <p>The blockquote element represents content that is quoted from another source, optionally with
   * a citation which must be within a footer or cite element, and optionally with in-line changes
   * such as annotations and abbreviations.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/blockquote">MDN:
   * &lt;blockquote&gt;</a>
   */
  public static BlockquoteTag blockquote(DomContent... children) {
    return new BlockquoteTag().with(children);
  }

  /**
   * Creates a <code>&lt;body&gt;</code> element.
   *
   * <p>The body element represents the content of the document.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/body">MDN:
   * &lt;body&gt;</a>
   */
  public static BodyTag body() {
    return new BodyTag();
  }

  /**
   * Creates a <code>&lt;body&gt;</code> element.
   *
   * <p>The body element represents the content of the document.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/body">MDN:
   * &lt;body&gt;</a>
   */
  public static BodyTag body(String text) {
    return new BodyTag().withText(text);
  }

  /**
   * Creates a <code>&lt;body&gt;</code> element.
   *
   * <p>The body element represents the content of the document.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/body">MDN:
   * &lt;body&gt;</a>
   */
  public static BodyTag body(DomContent... children) {
    return new BodyTag().with(children);
  }

  /**
   * Creates a <code>&lt;br&gt;</code> element.
   *
   * <p>The br element represents a line break.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/br">MDN:
   * &lt;br&gt;</a>
   */
  public static BrTag br() {
    return new BrTag();
  }

  /**
   * Creates a <code>&lt;button&gt;</code> element.
   *
   * <p>The button element represents a button labeled by its contents.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/button">MDN:
   * &lt;button&gt;</a>
   */
  public static ButtonTag button() {
    return new ButtonTag();
  }

  /**
   * Creates a <code>&lt;button&gt;</code> element.
   *
   * <p>The button element represents a button labeled by its contents.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/button">MDN:
   * &lt;button&gt;</a>
   */
  public static ButtonTag button(String text) {
    return new ButtonTag().withText(text);
  }

  /**
   * Creates a <code>&lt;button&gt;</code> element.
   *
   * <p>The button element represents a button labeled by its contents.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/button">MDN:
   * &lt;button&gt;</a>
   */
  public static ButtonTag button(DomContent... children) {
    return new ButtonTag().with(children);
  }

  /**
   * Creates a <code>&lt;canvas&gt;</code> element.
   *
   * <p>The canvas element provides scripts with a resolution-dependent bitmap canvas, which can be
   * used for rendering graphs, game graphics, art, or other visual images on the fly.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/canvas">MDN:
   * &lt;canvas&gt;</a>
   */
  public static CanvasTag canvas() {
    return new CanvasTag();
  }

  /**
   * Creates a <code>&lt;canvas&gt;</code> element.
   *
   * <p>The canvas element provides scripts with a resolution-dependent bitmap canvas, which can be
   * used for rendering graphs, game graphics, art, or other visual images on the fly.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/canvas">MDN:
   * &lt;canvas&gt;</a>
   */
  public static CanvasTag canvas(String text) {
    return new CanvasTag().withText(text);
  }

  /**
   * Creates a <code>&lt;canvas&gt;</code> element.
   *
   * <p>The canvas element provides scripts with a resolution-dependent bitmap canvas, which can be
   * used for rendering graphs, game graphics, art, or other visual images on the fly.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/canvas">MDN:
   * &lt;canvas&gt;</a>
   */
  public static CanvasTag canvas(DomContent... children) {
    return new CanvasTag().with(children);
  }

  /**
   * Creates a <code>&lt;caption&gt;</code> element.
   *
   * <p>The caption element represents the title of the table that is its parent, if it has a parent
   * and that is a table element.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/caption">MDN:
   * &lt;caption&gt;</a>
   */
  public static CaptionTag caption() {
    return new CaptionTag();
  }

  /**
   * Creates a <code>&lt;caption&gt;</code> element.
   *
   * <p>The caption element represents the title of the table that is its parent, if it has a parent
   * and that is a table element.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/caption">MDN:
   * &lt;caption&gt;</a>
   */
  public static CaptionTag caption(String text) {
    return new CaptionTag().withText(text);
  }

  /**
   * Creates a <code>&lt;caption&gt;</code> element.
   *
   * <p>The caption element represents the title of the table that is its parent, if it has a parent
   * and that is a table element.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/caption">MDN:
   * &lt;caption&gt;</a>
   */
  public static CaptionTag caption(DomContent... children) {
    return new CaptionTag().with(children);
  }

  /**
   * Creates a <code>&lt;cite&gt;</code> element.
   *
   * <p>The cite element represents a reference to a creative work.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/cite">MDN:
   * &lt;cite&gt;</a>
   */
  public static CiteTag cite() {
    return new CiteTag();
  }

  /**
   * Creates a <code>&lt;cite&gt;</code> element.
   *
   * <p>The cite element represents a reference to a creative work.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/cite">MDN:
   * &lt;cite&gt;</a>
   */
  public static CiteTag cite(String text) {
    return new CiteTag().withText(text);
  }

  /**
   * Creates a <code>&lt;cite&gt;</code> element.
   *
   * <p>The cite element represents a reference to a creative work.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/cite">MDN:
   * &lt;cite&gt;</a>
   */
  public static CiteTag cite(DomContent... children) {
    return new CiteTag().with(children);
  }

  /**
   * Creates a <code>&lt;code&gt;</code> element.
   *
   * <p>The code element represents a fragment of computer code.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/code">MDN:
   * &lt;code&gt;</a>
   */
  public static CodeTag code() {
    return new CodeTag();
  }

  /**
   * Creates a <code>&lt;code&gt;</code> element.
   *
   * <p>The code element represents a fragment of computer code.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/code">MDN:
   * &lt;code&gt;</a>
   */
  public static CodeTag code(String text) {
    return new CodeTag().withText(text);
  }

  /**
   * Creates a <code>&lt;code&gt;</code> element.
   *
   * <p>The code element represents a fragment of computer code.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/code">MDN:
   * &lt;code&gt;</a>
   */
  public static CodeTag code(DomContent... children) {
    return new CodeTag().with(children);
  }

  /**
   * Creates a <code>&lt;col&gt;</code> element.
   *
   * <p>If a col element has a parent and that is a colgroup element that itself has a parent that is
   * a table element, then the col element represents one or more columns in the column group
   * represented by that colgroup.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/col">MDN:
   * &lt;col&gt;</a>
   */
  public static ColTag col() {
    return new ColTag();
  }

  /**
   * Creates a <code>&lt;colgroup&gt;</code> element.
   *
   * <p>The colgroup element represents a group of one or more columns in the table that is its
   * parent, if it has a parent and that is a table element.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/colgroup">MDN:
   * &lt;colgroup&gt;</a>
   */
  public static ColgroupTag colgroup() {
    return new ColgroupTag();
  }

  /**
   * Creates a <code>&lt;colgroup&gt;</code> element.
   *
   * <p>The colgroup element represents a group of one or more columns in the table that is its
   * parent, if it has a parent and that is a table element.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/colgroup">MDN:
   * &lt;colgroup&gt;</a>
   */
  public static ColgroupTag colgroup(String text) {
    return new ColgroupTag().withText(text);
  }

  /**
   * Creates a <code>&lt;colgroup&gt;</code> element.
   *
   * <p>The colgroup element represents a group of one or more columns in the table that is its
   * parent, if it has a parent and that is a table element.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/colgroup">MDN:
   * &lt;colgroup&gt;</a>
   */
  public static ColgroupTag colgroup(DomContent... children) {
    return new ColgroupTag().with(children);
  }

  /**
   * Creates a <code>&lt;data&gt;</code> element.
   *
   * <p>The data element links a given piece of content with a machine-readable translation.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/data">MDN:
   * &lt;data&gt;</a>
   */
  public static DataTag data() {
    return new DataTag();
  }

  /**
   * Creates a <code>&lt;data&gt;</code> element.
   *
   * <p>The data element links a given piece of content with a machine-readable translation.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/data">MDN:
   * &lt;data&gt;</a>
   */
  public static DataTag data(String text) {
    return new DataTag().withText(text);
  }

  /**
   * Creates a <code>&lt;data&gt;</code> element.
   *
   * <p>The data element links a given piece of content with a machine-readable translation.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/data">MDN:
   * &lt;data&gt;</a>
   */
  public static DataTag data(DomContent... children) {
    return new DataTag().with(children);
  }

  /**
   * Creates a <code>&lt;datalist&gt;</code> element.
   *
   * <p>The datalist element represents a set of option elements that represent predefined options
   * for other controls.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/datalist">MDN:
   * &lt;datalist&gt;</a>
   */
  public static DatalistTag datalist() {
    return new DatalistTag();
  }

  /**
   * Creates a <code>&lt;datalist&gt;</code> element.
   *
   * <p>The datalist element represents a set of option elements that represent predefined options
   * for other controls.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/datalist">MDN:
   * &lt;datalist&gt;</a>
   */
  public static DatalistTag datalist(String text) {
    return new DatalistTag().withText(text);
  }

  /**
   * Creates a <code>&lt;datalist&gt;</code> element.
   *
   * <p>The datalist element represents a set of option elements that represent predefined options
   * for other controls.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/datalist">MDN:
   * &lt;datalist&gt;</a>
   */
  public static DatalistTag datalist(DomContent... children) {
    return new DatalistTag().with(children);
  }

  /**
   * Creates a <code>&lt;dd&gt;</code> element.
   *
   * <p>The dd element represents the description, definition, or value, part of a term-description
   * group in a description list (dl element).
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/dd">MDN:
   * &lt;dd&gt;</a>
   */
  public static DdTag dd() {
    return new DdTag();
  }

  /**
   * Creates a <code>&lt;dd&gt;</code> element.
   *
   * <p>The dd element represents the description, definition, or value, part of a term-description
   * group in a description list (dl element).
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/dd">MDN:
   * &lt;dd&gt;</a>
   */
  public static DdTag dd(String text) {
    return new DdTag().withText(text);
  }

  /**
   * Creates a <code>&lt;dd&gt;</code> element.
   *
   * <p>The dd element represents the description, definition, or value, part of a term-description
   * group in a description list (dl element).
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/dd">MDN:
   * &lt;dd&gt;</a>
   */
  public static DdTag dd(DomContent... children) {
    return new DdTag().with(children);
  }

  /**
   * Creates a <code>&lt;del&gt;</code> element.
   *
   * <p>The del element represents a removal from the document.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/del">MDN:
   * &lt;del&gt;</a>
   */
  public static DelTag del() {
    return new DelTag();
  }

  /**
   * Creates a <code>&lt;del&gt;</code> element.
   *
   * <p>The del element represents a removal from the document.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/del">MDN:
   * &lt;del&gt;</a>
   */
  public static DelTag del(String text) {
    return new DelTag().withText(text);
  }

  /**
   * Creates a <code>&lt;del&gt;</code> element.
   *
   * <p>The del element represents a removal from the document.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/del">MDN:
   * &lt;del&gt;</a>
   */
  public static DelTag del(DomContent... children) {
    return new DelTag().with(children);
  }

  /**
   * Creates a <code>&lt;details&gt;</code> element.
   *
   * <p>The details element represents a disclosure widget from which the user can obtain additional
   * information or controls.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/details">MDN:
   * &lt;details&gt;</a>
   */
  public static DetailsTag details() {
    return new DetailsTag();
  }

  /**
   * Creates a <code>&lt;details&gt;</code> element.
   *
   * <p>The details element represents a disclosure widget from which the user can obtain additional
   * information or controls.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/details">MDN:
   * &lt;details&gt;</a>
   */
  public static DetailsTag details(String text) {
    return new DetailsTag().withText(text);
  }

  /**
   * Creates a <code>&lt;details&gt;</code> element.
   *
   * <p>The details element represents a disclosure widget from which the user can obtain additional
   * information or controls.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/details">MDN:
   * &lt;details&gt;</a>
   */
  public static DetailsTag details(DomContent... children) {
    return new DetailsTag().with(children);
  }

  /**
   * Creates a <code>&lt;dfn&gt;</code> element.
   *
   * <p>The dfn element represents the defining instance of a term.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/dfn">MDN:
   * &lt;dfn&gt;</a>
   */
  public static DfnTag dfn() {
    return new DfnTag();
  }

  /**
   * Creates a <code>&lt;dfn&gt;</code> element.
   *
   * <p>The dfn element represents the defining instance of a term.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/dfn">MDN:
   * &lt;dfn&gt;</a>
   */
  public static DfnTag dfn(String text) {
    return new DfnTag().withText(text);
  }

  /**
   * Creates a <code>&lt;dfn&gt;</code> element.
   *
   * <p>The dfn element represents the defining instance of a term.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/dfn">MDN:
   * &lt;dfn&gt;</a>
   */
  public static DfnTag dfn(DomContent... children) {
    return new DfnTag().with(children);
  }

  /**
   * Creates a <code>&lt;dialog&gt;</code> element.
   *
   * <p>The dialog element represents a part of an application that a user interacts with to perform
   * a task, for example a dialog box, inspector, or window.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/dialog">MDN:
   * &lt;dialog&gt;</a>
   */
  public static DialogTag dialog() {
    return new DialogTag();
  }

  /**
   * Creates a <code>&lt;dialog&gt;</code> element.
   *
   * <p>The dialog element represents a part of an application that a user interacts with to perform
   * a task, for example a dialog box, inspector, or window.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/dialog">MDN:
   * &lt;dialog&gt;</a>
   */
  public static DialogTag dialog(String text) {
    return new DialogTag().withText(text);
  }

  /**
   * Creates a <code>&lt;dialog&gt;</code> element.
   *
   * <p>The dialog element represents a part of an application that a user interacts with to perform
   * a task, for example a dialog box, inspector, or window.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/dialog">MDN:
   * &lt;dialog&gt;</a>
   */
  public static DialogTag dialog(DomContent... children) {
    return new DialogTag().with(children);
  }

  /**
   * Creates a <code>&lt;div&gt;</code> element.
   *
   * <p>The div element has no special meaning at all.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/div">MDN:
   * &lt;div&gt;</a>
   */
  public static DivTag div() {
    return new DivTag();
  }

  /**
   * Creates a <code>&lt;div&gt;</code> element.
   *
   * <p>The div element has no special meaning at all.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/div">MDN:
   * &lt;div&gt;</a>
   */
  public static DivTag div(String text) {
    return new DivTag().withText(text);
  }

  /**
   * Creates a <code>&lt;div&gt;</code> element.
   *
   * <p>The div element has no special meaning at all.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/div">MDN:
   * &lt;div&gt;</a>
   */
  public static DivTag div(DomContent... children) {
    return new DivTag().with(children);
  }

  /**
   * Creates a <code>&lt;dl&gt;</code> element.
   *
   * <p>The dl element represents an association list consisting of zero or more name-value groups (a
   * description list).
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/dl">MDN:
   * &lt;dl&gt;</a>
   */
  public static DlTag dl() {
    return new DlTag();
  }

  /**
   * Creates a <code>&lt;dl&gt;</code> element.
   *
   * <p>The dl element represents an association list consisting of zero or more name-value groups (a
   * description list).
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/dl">MDN:
   * &lt;dl&gt;</a>
   */
  public static DlTag dl(String text) {
    return new DlTag().withText(text);
  }

  /**
   * Creates a <code>&lt;dl&gt;</code> element.
   *
   * <p>The dl element represents an association list consisting of zero or more name-value groups (a
   * description list).
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/dl">MDN:
   * &lt;dl&gt;</a>
   */
  public static DlTag dl(DomContent... children) {
    return new DlTag().with(children);
  }

  /**
   * Creates a <code>&lt;dt&gt;</code> element.
   *
   * <p>The dt element represents the term, or name, part of a term-description group in a
   * description list (dl element).
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/dt">MDN:
   * &lt;dt&gt;</a>
   */
  public static DtTag dt() {
    return new DtTag();
  }

  /**
   * Creates a <code>&lt;dt&gt;</code> element.
   *
   * <p>The dt element represents the term, or name, part of a term-description group in a
   * description list (dl element).
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/dt">MDN:
   * &lt;dt&gt;</a>
   */
  public static DtTag dt(String text) {
    return new DtTag().withText(text);
  }

  /**
   * Creates a <code>&lt;dt&gt;</code> element.
   *
   * <p>The dt element represents the term, or name, part of a term-description group in a
   * description list (dl element).
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/dt">MDN:
   * &lt;dt&gt;</a>
   */
  public static DtTag dt(DomContent... children) {
    return new DtTag().with(children);
  }

  /**
   * Creates a <code>&lt;em&gt;</code> element.
   *
   * <p>The em element represents stress emphasis of its contents.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/em">MDN:
   * &lt;em&gt;</a>
   */
  public static EmTag em() {
    return new EmTag();
  }

  /**
   * Creates a <code>&lt;em&gt;</code> element.
   *
   * <p>The em element represents stress emphasis of its contents.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/em">MDN:
   * &lt;em&gt;</a>
   */
  public static EmTag em(String text) {
    return new EmTag().withText(text);
  }

  /**
   * Creates a <code>&lt;em&gt;</code> element.
   *
   * <p>The em element represents stress emphasis of its contents.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/em">MDN:
   * &lt;em&gt;</a>
   */
  public static EmTag em(DomContent... children) {
    return new EmTag().with(children);
  }

  /**
   * Creates a <code>&lt;embed&gt;</code> element.
   *
   * <p>The embed element provides an integration point for an external (typically non-HTML)
   * application or interactive content.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/embed">MDN:
   * &lt;embed&gt;</a>
   */
  public static EmbedTag embed() {
    return new EmbedTag();
  }

  /**
   * Creates a <code>&lt;fencedframe&gt;</code> element.
   *
   * <p>The fencedframe element represents a nested browsing context, embedding another HTML page
   * into the current one.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/fencedframe">MDN:
   * &lt;fencedframe&gt;</a>
   * @deprecated The <code>&lt;fencedframe&gt;</code> element is marked deprecated in MDN
   * browser-compat-data; avoid in new markup.
   */
  @Deprecated
  public static FencedframeTag fencedframe() {
    return new FencedframeTag();
  }

  /**
   * Creates a <code>&lt;fencedframe&gt;</code> element.
   *
   * <p>The fencedframe element represents a nested browsing context, embedding another HTML page
   * into the current one.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/fencedframe">MDN:
   * &lt;fencedframe&gt;</a>
   * @deprecated The <code>&lt;fencedframe&gt;</code> element is marked deprecated in MDN
   * browser-compat-data; avoid in new markup.
   */
  @Deprecated
  public static FencedframeTag fencedframe(String text) {
    return new FencedframeTag().withText(text);
  }

  /**
   * Creates a <code>&lt;fencedframe&gt;</code> element.
   *
   * <p>The fencedframe element represents a nested browsing context, embedding another HTML page
   * into the current one.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/fencedframe">MDN:
   * &lt;fencedframe&gt;</a>
   * @deprecated The <code>&lt;fencedframe&gt;</code> element is marked deprecated in MDN
   * browser-compat-data; avoid in new markup.
   */
  @Deprecated
  public static FencedframeTag fencedframe(DomContent... children) {
    return new FencedframeTag().with(children);
  }

  /**
   * Creates a <code>&lt;fieldset&gt;</code> element.
   *
   * <p>The fieldset element represents a set of form controls optionally grouped under a common
   * name.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/fieldset">MDN:
   * &lt;fieldset&gt;</a>
   */
  public static FieldsetTag fieldset() {
    return new FieldsetTag();
  }

  /**
   * Creates a <code>&lt;fieldset&gt;</code> element.
   *
   * <p>The fieldset element represents a set of form controls optionally grouped under a common
   * name.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/fieldset">MDN:
   * &lt;fieldset&gt;</a>
   */
  public static FieldsetTag fieldset(String text) {
    return new FieldsetTag().withText(text);
  }

  /**
   * Creates a <code>&lt;fieldset&gt;</code> element.
   *
   * <p>The fieldset element represents a set of form controls optionally grouped under a common
   * name.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/fieldset">MDN:
   * &lt;fieldset&gt;</a>
   */
  public static FieldsetTag fieldset(DomContent... children) {
    return new FieldsetTag().with(children);
  }

  /**
   * Creates a <code>&lt;figcaption&gt;</code> element.
   *
   * <p>The figcaption element represents a caption or legend for the rest of the contents of the
   * figcaption element's parent figure element, if any.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/figcaption">MDN:
   * &lt;figcaption&gt;</a>
   */
  public static FigcaptionTag figcaption() {
    return new FigcaptionTag();
  }

  /**
   * Creates a <code>&lt;figcaption&gt;</code> element.
   *
   * <p>The figcaption element represents a caption or legend for the rest of the contents of the
   * figcaption element's parent figure element, if any.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/figcaption">MDN:
   * &lt;figcaption&gt;</a>
   */
  public static FigcaptionTag figcaption(String text) {
    return new FigcaptionTag().withText(text);
  }

  /**
   * Creates a <code>&lt;figcaption&gt;</code> element.
   *
   * <p>The figcaption element represents a caption or legend for the rest of the contents of the
   * figcaption element's parent figure element, if any.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/figcaption">MDN:
   * &lt;figcaption&gt;</a>
   */
  public static FigcaptionTag figcaption(DomContent... children) {
    return new FigcaptionTag().with(children);
  }

  /**
   * Creates a <code>&lt;figure&gt;</code> element.
   *
   * <p>The figure element represents some flow content, optionally with a caption, that is
   * self-contained (like a complete sentence) and is typically referenced as a single unit from the
   * main flow of the document.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/figure">MDN:
   * &lt;figure&gt;</a>
   */
  public static FigureTag figure() {
    return new FigureTag();
  }

  /**
   * Creates a <code>&lt;figure&gt;</code> element.
   *
   * <p>The figure element represents some flow content, optionally with a caption, that is
   * self-contained (like a complete sentence) and is typically referenced as a single unit from the
   * main flow of the document.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/figure">MDN:
   * &lt;figure&gt;</a>
   */
  public static FigureTag figure(String text) {
    return new FigureTag().withText(text);
  }

  /**
   * Creates a <code>&lt;figure&gt;</code> element.
   *
   * <p>The figure element represents some flow content, optionally with a caption, that is
   * self-contained (like a complete sentence) and is typically referenced as a single unit from the
   * main flow of the document.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/figure">MDN:
   * &lt;figure&gt;</a>
   */
  public static FigureTag figure(DomContent... children) {
    return new FigureTag().with(children);
  }

  /**
   * Creates a <code>&lt;footer&gt;</code> element.
   *
   * <p>The footer element represents a footer for its nearest ancestor sectioning content or
   * sectioning root element.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/footer">MDN:
   * &lt;footer&gt;</a>
   */
  public static FooterTag footer() {
    return new FooterTag();
  }

  /**
   * Creates a <code>&lt;footer&gt;</code> element.
   *
   * <p>The footer element represents a footer for its nearest ancestor sectioning content or
   * sectioning root element.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/footer">MDN:
   * &lt;footer&gt;</a>
   */
  public static FooterTag footer(String text) {
    return new FooterTag().withText(text);
  }

  /**
   * Creates a <code>&lt;footer&gt;</code> element.
   *
   * <p>The footer element represents a footer for its nearest ancestor sectioning content or
   * sectioning root element.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/footer">MDN:
   * &lt;footer&gt;</a>
   */
  public static FooterTag footer(DomContent... children) {
    return new FooterTag().with(children);
  }

  /**
   * Creates a <code>&lt;form&gt;</code> element.
   *
   * <p>The form element represents a collection of form-associated elements, some of which can
   * represent editable values that can be submitted to a server for processing.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/form">MDN:
   * &lt;form&gt;</a>
   */
  public static FormTag form() {
    return new FormTag();
  }

  /**
   * Creates a <code>&lt;form&gt;</code> element.
   *
   * <p>The form element represents a collection of form-associated elements, some of which can
   * represent editable values that can be submitted to a server for processing.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/form">MDN:
   * &lt;form&gt;</a>
   */
  public static FormTag form(String text) {
    return new FormTag().withText(text);
  }

  /**
   * Creates a <code>&lt;form&gt;</code> element.
   *
   * <p>The form element represents a collection of form-associated elements, some of which can
   * represent editable values that can be submitted to a server for processing.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/form">MDN:
   * &lt;form&gt;</a>
   */
  public static FormTag form(DomContent... children) {
    return new FormTag().with(children);
  }

  /**
   * Creates a <code>&lt;h1&gt;</code> element.
   *
   * <p>The h1 element represents a section heading.
   *
   * @see <a
   * href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/Heading_Elements">MDN:
   * &lt;h1&gt;</a>
   */
  public static H1Tag h1() {
    return new H1Tag();
  }

  /**
   * Creates a <code>&lt;h1&gt;</code> element.
   *
   * <p>The h1 element represents a section heading.
   *
   * @see <a
   * href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/Heading_Elements">MDN:
   * &lt;h1&gt;</a>
   */
  public static H1Tag h1(String text) {
    return new H1Tag().withText(text);
  }

  /**
   * Creates a <code>&lt;h1&gt;</code> element.
   *
   * <p>The h1 element represents a section heading.
   *
   * @see <a
   * href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/Heading_Elements">MDN:
   * &lt;h1&gt;</a>
   */
  public static H1Tag h1(DomContent... children) {
    return new H1Tag().with(children);
  }

  /**
   * Creates a <code>&lt;h2&gt;</code> element.
   *
   * <p>The h2 element represents a section heading.
   *
   * @see <a
   * href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/Heading_Elements">MDN:
   * &lt;h2&gt;</a>
   */
  public static H2Tag h2() {
    return new H2Tag();
  }

  /**
   * Creates a <code>&lt;h2&gt;</code> element.
   *
   * <p>The h2 element represents a section heading.
   *
   * @see <a
   * href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/Heading_Elements">MDN:
   * &lt;h2&gt;</a>
   */
  public static H2Tag h2(String text) {
    return new H2Tag().withText(text);
  }

  /**
   * Creates a <code>&lt;h2&gt;</code> element.
   *
   * <p>The h2 element represents a section heading.
   *
   * @see <a
   * href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/Heading_Elements">MDN:
   * &lt;h2&gt;</a>
   */
  public static H2Tag h2(DomContent... children) {
    return new H2Tag().with(children);
  }

  /**
   * Creates a <code>&lt;h3&gt;</code> element.
   *
   * <p>The h3 element represents a section heading.
   *
   * @see <a
   * href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/Heading_Elements">MDN:
   * &lt;h3&gt;</a>
   */
  public static H3Tag h3() {
    return new H3Tag();
  }

  /**
   * Creates a <code>&lt;h3&gt;</code> element.
   *
   * <p>The h3 element represents a section heading.
   *
   * @see <a
   * href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/Heading_Elements">MDN:
   * &lt;h3&gt;</a>
   */
  public static H3Tag h3(String text) {
    return new H3Tag().withText(text);
  }

  /**
   * Creates a <code>&lt;h3&gt;</code> element.
   *
   * <p>The h3 element represents a section heading.
   *
   * @see <a
   * href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/Heading_Elements">MDN:
   * &lt;h3&gt;</a>
   */
  public static H3Tag h3(DomContent... children) {
    return new H3Tag().with(children);
  }

  /**
   * Creates a <code>&lt;h4&gt;</code> element.
   *
   * <p>The h4 element represents a section heading.
   *
   * @see <a
   * href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/Heading_Elements">MDN:
   * &lt;h4&gt;</a>
   */
  public static H4Tag h4() {
    return new H4Tag();
  }

  /**
   * Creates a <code>&lt;h4&gt;</code> element.
   *
   * <p>The h4 element represents a section heading.
   *
   * @see <a
   * href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/Heading_Elements">MDN:
   * &lt;h4&gt;</a>
   */
  public static H4Tag h4(String text) {
    return new H4Tag().withText(text);
  }

  /**
   * Creates a <code>&lt;h4&gt;</code> element.
   *
   * <p>The h4 element represents a section heading.
   *
   * @see <a
   * href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/Heading_Elements">MDN:
   * &lt;h4&gt;</a>
   */
  public static H4Tag h4(DomContent... children) {
    return new H4Tag().with(children);
  }

  /**
   * Creates a <code>&lt;h5&gt;</code> element.
   *
   * <p>The h5 element represents a section heading.
   *
   * @see <a
   * href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/Heading_Elements">MDN:
   * &lt;h5&gt;</a>
   */
  public static H5Tag h5() {
    return new H5Tag();
  }

  /**
   * Creates a <code>&lt;h5&gt;</code> element.
   *
   * <p>The h5 element represents a section heading.
   *
   * @see <a
   * href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/Heading_Elements">MDN:
   * &lt;h5&gt;</a>
   */
  public static H5Tag h5(String text) {
    return new H5Tag().withText(text);
  }

  /**
   * Creates a <code>&lt;h5&gt;</code> element.
   *
   * <p>The h5 element represents a section heading.
   *
   * @see <a
   * href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/Heading_Elements">MDN:
   * &lt;h5&gt;</a>
   */
  public static H5Tag h5(DomContent... children) {
    return new H5Tag().with(children);
  }

  /**
   * Creates a <code>&lt;h6&gt;</code> element.
   *
   * <p>The h6 element represents a section heading.
   *
   * @see <a
   * href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/Heading_Elements">MDN:
   * &lt;h6&gt;</a>
   */
  public static H6Tag h6() {
    return new H6Tag();
  }

  /**
   * Creates a <code>&lt;h6&gt;</code> element.
   *
   * <p>The h6 element represents a section heading.
   *
   * @see <a
   * href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/Heading_Elements">MDN:
   * &lt;h6&gt;</a>
   */
  public static H6Tag h6(String text) {
    return new H6Tag().withText(text);
  }

  /**
   * Creates a <code>&lt;h6&gt;</code> element.
   *
   * <p>The h6 element represents a section heading.
   *
   * @see <a
   * href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/Heading_Elements">MDN:
   * &lt;h6&gt;</a>
   */
  public static H6Tag h6(DomContent... children) {
    return new H6Tag().with(children);
  }

  /**
   * Creates a <code>&lt;head&gt;</code> element.
   *
   * <p>The head element represents a collection of metadata for the Document.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/head">MDN:
   * &lt;head&gt;</a>
   */
  public static HeadTag head() {
    return new HeadTag();
  }

  /**
   * Creates a <code>&lt;head&gt;</code> element.
   *
   * <p>The head element represents a collection of metadata for the Document.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/head">MDN:
   * &lt;head&gt;</a>
   */
  public static HeadTag head(String text) {
    return new HeadTag().withText(text);
  }

  /**
   * Creates a <code>&lt;head&gt;</code> element.
   *
   * <p>The head element represents a collection of metadata for the Document.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/head">MDN:
   * &lt;head&gt;</a>
   */
  public static HeadTag head(DomContent... children) {
    return new HeadTag().with(children);
  }

  /**
   * Creates a <code>&lt;header&gt;</code> element.
   *
   * <p>The header element represents introductory content for its nearest ancestor sectioning
   * content or sectioning root element.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/header">MDN:
   * &lt;header&gt;</a>
   */
  public static HeaderTag header() {
    return new HeaderTag();
  }

  /**
   * Creates a <code>&lt;header&gt;</code> element.
   *
   * <p>The header element represents introductory content for its nearest ancestor sectioning
   * content or sectioning root element.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/header">MDN:
   * &lt;header&gt;</a>
   */
  public static HeaderTag header(String text) {
    return new HeaderTag().withText(text);
  }

  /**
   * Creates a <code>&lt;header&gt;</code> element.
   *
   * <p>The header element represents introductory content for its nearest ancestor sectioning
   * content or sectioning root element.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/header">MDN:
   * &lt;header&gt;</a>
   */
  public static HeaderTag header(DomContent... children) {
    return new HeaderTag().with(children);
  }

  /**
   * Creates a <code>&lt;hgroup&gt;</code> element.
   *
   * <p>The hgroup element represents a heading and related content.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/hgroup">MDN:
   * &lt;hgroup&gt;</a>
   */
  public static HgroupTag hgroup() {
    return new HgroupTag();
  }

  /**
   * Creates a <code>&lt;hgroup&gt;</code> element.
   *
   * <p>The hgroup element represents a heading and related content.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/hgroup">MDN:
   * &lt;hgroup&gt;</a>
   */
  public static HgroupTag hgroup(String text) {
    return new HgroupTag().withText(text);
  }

  /**
   * Creates a <code>&lt;hgroup&gt;</code> element.
   *
   * <p>The hgroup element represents a heading and related content.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/hgroup">MDN:
   * &lt;hgroup&gt;</a>
   */
  public static HgroupTag hgroup(DomContent... children) {
    return new HgroupTag().with(children);
  }

  /**
   * Creates a <code>&lt;hr&gt;</code> element.
   *
   * <p>The hr element represents a paragraph-level thematic break, e.g.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/hr">MDN:
   * &lt;hr&gt;</a>
   */
  public static HrTag hr() {
    return new HrTag();
  }

  /**
   * Creates a <code>&lt;html&gt;</code> element.
   *
   * <p>The html element represents the root of an HTML document.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/html">MDN:
   * &lt;html&gt;</a>
   */
  public static HtmlTag html() {
    return new HtmlTag();
  }

  /**
   * Creates a <code>&lt;html&gt;</code> element.
   *
   * <p>The html element represents the root of an HTML document.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/html">MDN:
   * &lt;html&gt;</a>
   */
  public static HtmlTag html(String text) {
    return new HtmlTag().withText(text);
  }

  /**
   * Creates a <code>&lt;html&gt;</code> element.
   *
   * <p>The html element represents the root of an HTML document.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/html">MDN:
   * &lt;html&gt;</a>
   */
  public static HtmlTag html(DomContent... children) {
    return new HtmlTag().with(children);
  }

  /**
   * Creates a <code>&lt;i&gt;</code> element.
   *
   * <p>The i element represents a span of text in an alternate voice or mood, or otherwise offset
   * from the normal prose in a manner indicating a different quality of text, such as a taxonomic
   * designation, a technical term, an idiomatic phrase from another language, transliteration, a
   * thought, or a ship name in Western texts.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/i">MDN:
   * &lt;i&gt;</a>
   */
  public static ITag i() {
    return new ITag();
  }

  /**
   * Creates a <code>&lt;i&gt;</code> element.
   *
   * <p>The i element represents a span of text in an alternate voice or mood, or otherwise offset
   * from the normal prose in a manner indicating a different quality of text, such as a taxonomic
   * designation, a technical term, an idiomatic phrase from another language, transliteration, a
   * thought, or a ship name in Western texts.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/i">MDN:
   * &lt;i&gt;</a>
   */
  public static ITag i(String text) {
    return new ITag().withText(text);
  }

  /**
   * Creates a <code>&lt;i&gt;</code> element.
   *
   * <p>The i element represents a span of text in an alternate voice or mood, or otherwise offset
   * from the normal prose in a manner indicating a different quality of text, such as a taxonomic
   * designation, a technical term, an idiomatic phrase from another language, transliteration, a
   * thought, or a ship name in Western texts.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/i">MDN:
   * &lt;i&gt;</a>
   */
  public static ITag i(DomContent... children) {
    return new ITag().with(children);
  }

  /**
   * Creates a <code>&lt;iframe&gt;</code> element.
   *
   * <p>The iframe element represents a nested browsing context.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/iframe">MDN:
   * &lt;iframe&gt;</a>
   */
  public static IframeTag iframe() {
    return new IframeTag();
  }

  /**
   * Creates a <code>&lt;iframe&gt;</code> element.
   *
   * <p>The iframe element represents a nested browsing context.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/iframe">MDN:
   * &lt;iframe&gt;</a>
   */
  public static IframeTag iframe(String text) {
    return new IframeTag().withText(text);
  }

  /**
   * Creates a <code>&lt;iframe&gt;</code> element.
   *
   * <p>The iframe element represents a nested browsing context.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/iframe">MDN:
   * &lt;iframe&gt;</a>
   */
  public static IframeTag iframe(DomContent... children) {
    return new IframeTag().with(children);
  }

  /**
   * Creates a <code>&lt;img&gt;</code> element.
   *
   * <p>An img element represents an image.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/img">MDN:
   * &lt;img&gt;</a>
   */
  public static ImgTag img() {
    return new ImgTag();
  }

  /**
   * Creates a <code>&lt;input&gt;</code> element.
   *
   * <p>The input element represents a typed data field, usually with a form control to allow the
   * user to edit the data.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/input">MDN:
   * &lt;input&gt;</a>
   */
  public static InputTag input() {
    return new InputTag();
  }

  /**
   * Creates a <code>&lt;ins&gt;</code> element.
   *
   * <p>The ins element represents an addition to the document.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/ins">MDN:
   * &lt;ins&gt;</a>
   */
  public static InsTag ins() {
    return new InsTag();
  }

  /**
   * Creates a <code>&lt;ins&gt;</code> element.
   *
   * <p>The ins element represents an addition to the document.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/ins">MDN:
   * &lt;ins&gt;</a>
   */
  public static InsTag ins(String text) {
    return new InsTag().withText(text);
  }

  /**
   * Creates a <code>&lt;ins&gt;</code> element.
   *
   * <p>The ins element represents an addition to the document.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/ins">MDN:
   * &lt;ins&gt;</a>
   */
  public static InsTag ins(DomContent... children) {
    return new InsTag().with(children);
  }

  /**
   * Creates a <code>&lt;kbd&gt;</code> element.
   *
   * <p>The kbd element represents user input (typically keyboard input, although it may also be used
   * to represent other input, such as voice commands).
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/kbd">MDN:
   * &lt;kbd&gt;</a>
   */
  public static KbdTag kbd() {
    return new KbdTag();
  }

  /**
   * Creates a <code>&lt;kbd&gt;</code> element.
   *
   * <p>The kbd element represents user input (typically keyboard input, although it may also be used
   * to represent other input, such as voice commands).
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/kbd">MDN:
   * &lt;kbd&gt;</a>
   */
  public static KbdTag kbd(String text) {
    return new KbdTag().withText(text);
  }

  /**
   * Creates a <code>&lt;kbd&gt;</code> element.
   *
   * <p>The kbd element represents user input (typically keyboard input, although it may also be used
   * to represent other input, such as voice commands).
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/kbd">MDN:
   * &lt;kbd&gt;</a>
   */
  public static KbdTag kbd(DomContent... children) {
    return new KbdTag().with(children);
  }

  /**
   * Creates a <code>&lt;label&gt;</code> element.
   *
   * <p>The label element represents a caption in a user interface.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/label">MDN:
   * &lt;label&gt;</a>
   */
  public static LabelTag label() {
    return new LabelTag();
  }

  /**
   * Creates a <code>&lt;label&gt;</code> element.
   *
   * <p>The label element represents a caption in a user interface.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/label">MDN:
   * &lt;label&gt;</a>
   */
  public static LabelTag label(String text) {
    return new LabelTag().withText(text);
  }

  /**
   * Creates a <code>&lt;label&gt;</code> element.
   *
   * <p>The label element represents a caption in a user interface.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/label">MDN:
   * &lt;label&gt;</a>
   */
  public static LabelTag label(DomContent... children) {
    return new LabelTag().with(children);
  }

  /**
   * Creates a <code>&lt;legend&gt;</code> element.
   *
   * <p>The legend element represents a caption for the rest of the contents of the legend element's
   * parent fieldset element, if any.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/legend">MDN:
   * &lt;legend&gt;</a>
   */
  public static LegendTag legend() {
    return new LegendTag();
  }

  /**
   * Creates a <code>&lt;legend&gt;</code> element.
   *
   * <p>The legend element represents a caption for the rest of the contents of the legend element's
   * parent fieldset element, if any.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/legend">MDN:
   * &lt;legend&gt;</a>
   */
  public static LegendTag legend(String text) {
    return new LegendTag().withText(text);
  }

  /**
   * Creates a <code>&lt;legend&gt;</code> element.
   *
   * <p>The legend element represents a caption for the rest of the contents of the legend element's
   * parent fieldset element, if any.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/legend">MDN:
   * &lt;legend&gt;</a>
   */
  public static LegendTag legend(DomContent... children) {
    return new LegendTag().with(children);
  }

  /**
   * Creates a <code>&lt;li&gt;</code> element.
   *
   * <p>The li element represents a list item.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/li">MDN:
   * &lt;li&gt;</a>
   */
  public static LiTag li() {
    return new LiTag();
  }

  /**
   * Creates a <code>&lt;li&gt;</code> element.
   *
   * <p>The li element represents a list item.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/li">MDN:
   * &lt;li&gt;</a>
   */
  public static LiTag li(String text) {
    return new LiTag().withText(text);
  }

  /**
   * Creates a <code>&lt;li&gt;</code> element.
   *
   * <p>The li element represents a list item.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/li">MDN:
   * &lt;li&gt;</a>
   */
  public static LiTag li(DomContent... children) {
    return new LiTag().with(children);
  }

  /**
   * Creates a <code>&lt;link&gt;</code> element.
   *
   * <p>The link element allows authors to link their document to other resources.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/link">MDN:
   * &lt;link&gt;</a>
   */
  public static LinkTag link() {
    return new LinkTag();
  }

  /**
   * Creates a <code>&lt;main&gt;</code> element.
   *
   * <p>The main element represents the main content of the body of a document or application.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/main">MDN:
   * &lt;main&gt;</a>
   */
  public static MainTag main() {
    return new MainTag();
  }

  /**
   * Creates a <code>&lt;main&gt;</code> element.
   *
   * <p>The main element represents the main content of the body of a document or application.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/main">MDN:
   * &lt;main&gt;</a>
   */
  public static MainTag main(String text) {
    return new MainTag().withText(text);
  }

  /**
   * Creates a <code>&lt;main&gt;</code> element.
   *
   * <p>The main element represents the main content of the body of a document or application.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/main">MDN:
   * &lt;main&gt;</a>
   */
  public static MainTag main(DomContent... children) {
    return new MainTag().with(children);
  }

  /**
   * Creates a <code>&lt;map&gt;</code> element.
   *
   * <p>The map element, in conjunction with an img element and any area element descendants, defines
   * an image map.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/map">MDN:
   * &lt;map&gt;</a>
   */
  public static MapTag map() {
    return new MapTag();
  }

  /**
   * Creates a <code>&lt;map&gt;</code> element.
   *
   * <p>The map element, in conjunction with an img element and any area element descendants, defines
   * an image map.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/map">MDN:
   * &lt;map&gt;</a>
   */
  public static MapTag map(String text) {
    return new MapTag().withText(text);
  }

  /**
   * Creates a <code>&lt;map&gt;</code> element.
   *
   * <p>The map element, in conjunction with an img element and any area element descendants, defines
   * an image map.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/map">MDN:
   * &lt;map&gt;</a>
   */
  public static MapTag map(DomContent... children) {
    return new MapTag().with(children);
  }

  /**
   * Creates a <code>&lt;mark&gt;</code> element.
   *
   * <p>The mark element represents a run of text in one document marked or highlighted for reference
   * purposes, due to its relevance in another context.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/mark">MDN:
   * &lt;mark&gt;</a>
   */
  public static MarkTag mark() {
    return new MarkTag();
  }

  /**
   * Creates a <code>&lt;mark&gt;</code> element.
   *
   * <p>The mark element represents a run of text in one document marked or highlighted for reference
   * purposes, due to its relevance in another context.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/mark">MDN:
   * &lt;mark&gt;</a>
   */
  public static MarkTag mark(String text) {
    return new MarkTag().withText(text);
  }

  /**
   * Creates a <code>&lt;mark&gt;</code> element.
   *
   * <p>The mark element represents a run of text in one document marked or highlighted for reference
   * purposes, due to its relevance in another context.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/mark">MDN:
   * &lt;mark&gt;</a>
   */
  public static MarkTag mark(DomContent... children) {
    return new MarkTag().with(children);
  }

  /**
   * Creates a <code>&lt;menu&gt;</code> element.
   *
   * <p>The menu element represents an unordered list of interactive items.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/menu">MDN:
   * &lt;menu&gt;</a>
   */
  public static MenuTag menu() {
    return new MenuTag();
  }

  /**
   * Creates a <code>&lt;menu&gt;</code> element.
   *
   * <p>The menu element represents an unordered list of interactive items.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/menu">MDN:
   * &lt;menu&gt;</a>
   */
  public static MenuTag menu(String text) {
    return new MenuTag().withText(text);
  }

  /**
   * Creates a <code>&lt;menu&gt;</code> element.
   *
   * <p>The menu element represents an unordered list of interactive items.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/menu">MDN:
   * &lt;menu&gt;</a>
   */
  public static MenuTag menu(DomContent... children) {
    return new MenuTag().with(children);
  }

  /**
   * Creates a <code>&lt;meta&gt;</code> element.
   *
   * <p>The meta element represents various kinds of metadata that cannot be expressed using the
   * title, base, link, style, and script elements.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/meta">MDN:
   * &lt;meta&gt;</a>
   */
  public static MetaTag meta() {
    return new MetaTag();
  }

  /**
   * Creates a <code>&lt;meter&gt;</code> element.
   *
   * <p>The meter element represents a scalar measurement within a known range, or a fractional
   * value; for example disk usage, the relevance of a query result, or the fraction of a voting
   * population to have selected a particular candidate.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/meter">MDN:
   * &lt;meter&gt;</a>
   */
  public static MeterTag meter() {
    return new MeterTag();
  }

  /**
   * Creates a <code>&lt;meter&gt;</code> element.
   *
   * <p>The meter element represents a scalar measurement within a known range, or a fractional
   * value; for example disk usage, the relevance of a query result, or the fraction of a voting
   * population to have selected a particular candidate.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/meter">MDN:
   * &lt;meter&gt;</a>
   */
  public static MeterTag meter(String text) {
    return new MeterTag().withText(text);
  }

  /**
   * Creates a <code>&lt;meter&gt;</code> element.
   *
   * <p>The meter element represents a scalar measurement within a known range, or a fractional
   * value; for example disk usage, the relevance of a query result, or the fraction of a voting
   * population to have selected a particular candidate.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/meter">MDN:
   * &lt;meter&gt;</a>
   */
  public static MeterTag meter(DomContent... children) {
    return new MeterTag().with(children);
  }

  /**
   * Creates a <code>&lt;nav&gt;</code> element.
   *
   * <p>The nav element represents a section of a page that links to other pages or to parts within
   * the page: a section with navigation links.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/nav">MDN:
   * &lt;nav&gt;</a>
   */
  public static NavTag nav() {
    return new NavTag();
  }

  /**
   * Creates a <code>&lt;nav&gt;</code> element.
   *
   * <p>The nav element represents a section of a page that links to other pages or to parts within
   * the page: a section with navigation links.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/nav">MDN:
   * &lt;nav&gt;</a>
   */
  public static NavTag nav(String text) {
    return new NavTag().withText(text);
  }

  /**
   * Creates a <code>&lt;nav&gt;</code> element.
   *
   * <p>The nav element represents a section of a page that links to other pages or to parts within
   * the page: a section with navigation links.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/nav">MDN:
   * &lt;nav&gt;</a>
   */
  public static NavTag nav(DomContent... children) {
    return new NavTag().with(children);
  }

  /**
   * Creates a <code>&lt;noscript&gt;</code> element.
   *
   * <p>The noscript element represents nothing if scripting is enabled, and represents its children
   * if scripting is disabled.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/noscript">MDN:
   * &lt;noscript&gt;</a>
   */
  public static NoscriptTag noscript() {
    return new NoscriptTag();
  }

  /**
   * Creates a <code>&lt;noscript&gt;</code> element.
   *
   * <p>The noscript element represents nothing if scripting is enabled, and represents its children
   * if scripting is disabled.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/noscript">MDN:
   * &lt;noscript&gt;</a>
   */
  public static NoscriptTag noscript(String text) {
    return new NoscriptTag().withText(text);
  }

  /**
   * Creates a <code>&lt;noscript&gt;</code> element.
   *
   * <p>The noscript element represents nothing if scripting is enabled, and represents its children
   * if scripting is disabled.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/noscript">MDN:
   * &lt;noscript&gt;</a>
   */
  public static NoscriptTag noscript(DomContent... children) {
    return new NoscriptTag().with(children);
  }

  /**
   * Creates a <code>&lt;object&gt;</code> element.
   *
   * <p>The object element can represent an external resource, which, depending on the type of the
   * resource, will either be treated as an image, as a nested browsing context, or as an external
   * resource to be processed by a plugin.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/object">MDN:
   * &lt;object&gt;</a>
   */
  public static ObjectTag object() {
    return new ObjectTag();
  }

  /**
   * Creates a <code>&lt;object&gt;</code> element.
   *
   * <p>The object element can represent an external resource, which, depending on the type of the
   * resource, will either be treated as an image, as a nested browsing context, or as an external
   * resource to be processed by a plugin.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/object">MDN:
   * &lt;object&gt;</a>
   */
  public static ObjectTag object(String text) {
    return new ObjectTag().withText(text);
  }

  /**
   * Creates a <code>&lt;object&gt;</code> element.
   *
   * <p>The object element can represent an external resource, which, depending on the type of the
   * resource, will either be treated as an image, as a nested browsing context, or as an external
   * resource to be processed by a plugin.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/object">MDN:
   * &lt;object&gt;</a>
   */
  public static ObjectTag object(DomContent... children) {
    return new ObjectTag().with(children);
  }

  /**
   * Creates a <code>&lt;ol&gt;</code> element.
   *
   * <p>The ol element represents a list of items, where the items have been intentionally ordered,
   * such that changing the order would change the meaning of the document.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/ol">MDN:
   * &lt;ol&gt;</a>
   */
  public static OlTag ol() {
    return new OlTag();
  }

  /**
   * Creates a <code>&lt;ol&gt;</code> element.
   *
   * <p>The ol element represents a list of items, where the items have been intentionally ordered,
   * such that changing the order would change the meaning of the document.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/ol">MDN:
   * &lt;ol&gt;</a>
   */
  public static OlTag ol(String text) {
    return new OlTag().withText(text);
  }

  /**
   * Creates a <code>&lt;ol&gt;</code> element.
   *
   * <p>The ol element represents a list of items, where the items have been intentionally ordered,
   * such that changing the order would change the meaning of the document.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/ol">MDN:
   * &lt;ol&gt;</a>
   */
  public static OlTag ol(DomContent... children) {
    return new OlTag().with(children);
  }

  /**
   * Creates a <code>&lt;optgroup&gt;</code> element.
   *
   * <p>The optgroup element represents a group of option elements with a common label.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/optgroup">MDN:
   * &lt;optgroup&gt;</a>
   */
  public static OptgroupTag optgroup() {
    return new OptgroupTag();
  }

  /**
   * Creates a <code>&lt;optgroup&gt;</code> element.
   *
   * <p>The optgroup element represents a group of option elements with a common label.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/optgroup">MDN:
   * &lt;optgroup&gt;</a>
   */
  public static OptgroupTag optgroup(String text) {
    return new OptgroupTag().withText(text);
  }

  /**
   * Creates a <code>&lt;optgroup&gt;</code> element.
   *
   * <p>The optgroup element represents a group of option elements with a common label.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/optgroup">MDN:
   * &lt;optgroup&gt;</a>
   */
  public static OptgroupTag optgroup(DomContent... children) {
    return new OptgroupTag().with(children);
  }

  /**
   * Creates a <code>&lt;option&gt;</code> element.
   *
   * <p>The option element represents an option in a select element or as part of a list of
   * suggestions in a datalist element.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/option">MDN:
   * &lt;option&gt;</a>
   */
  public static OptionTag option() {
    return new OptionTag();
  }

  /**
   * Creates a <code>&lt;option&gt;</code> element.
   *
   * <p>The option element represents an option in a select element or as part of a list of
   * suggestions in a datalist element.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/option">MDN:
   * &lt;option&gt;</a>
   */
  public static OptionTag option(String text) {
    return new OptionTag().withText(text);
  }

  /**
   * Creates a <code>&lt;option&gt;</code> element.
   *
   * <p>The option element represents an option in a select element or as part of a list of
   * suggestions in a datalist element.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/option">MDN:
   * &lt;option&gt;</a>
   */
  public static OptionTag option(DomContent... children) {
    return new OptionTag().with(children);
  }

  /**
   * Creates a <code>&lt;output&gt;</code> element.
   *
   * <p>The output element represents the result of a calculation performed by the application, or
   * the result of a user action.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/output">MDN:
   * &lt;output&gt;</a>
   */
  public static OutputTag output() {
    return new OutputTag();
  }

  /**
   * Creates a <code>&lt;output&gt;</code> element.
   *
   * <p>The output element represents the result of a calculation performed by the application, or
   * the result of a user action.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/output">MDN:
   * &lt;output&gt;</a>
   */
  public static OutputTag output(String text) {
    return new OutputTag().withText(text);
  }

  /**
   * Creates a <code>&lt;output&gt;</code> element.
   *
   * <p>The output element represents the result of a calculation performed by the application, or
   * the result of a user action.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/output">MDN:
   * &lt;output&gt;</a>
   */
  public static OutputTag output(DomContent... children) {
    return new OutputTag().with(children);
  }

  /**
   * Creates a <code>&lt;p&gt;</code> element.
   *
   * <p>The p element represents a paragraph.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/p">MDN:
   * &lt;p&gt;</a>
   */
  public static PTag p() {
    return new PTag();
  }

  /**
   * Creates a <code>&lt;p&gt;</code> element.
   *
   * <p>The p element represents a paragraph.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/p">MDN:
   * &lt;p&gt;</a>
   */
  public static PTag p(String text) {
    return new PTag().withText(text);
  }

  /**
   * Creates a <code>&lt;p&gt;</code> element.
   *
   * <p>The p element represents a paragraph.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/p">MDN:
   * &lt;p&gt;</a>
   */
  public static PTag p(DomContent... children) {
    return new PTag().with(children);
  }

  /**
   * Creates a <code>&lt;param&gt;</code> element.
   *
   * <p>The param element defines parameters for plugins invoked by object elements.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/param">MDN:
   * &lt;param&gt;</a>
   * @deprecated The <code>&lt;param&gt;</code> element is marked deprecated in MDN
   * browser-compat-data; avoid in new markup.
   */
  @Deprecated
  public static ParamTag param() {
    return new ParamTag();
  }

  /**
   * Creates a <code>&lt;picture&gt;</code> element.
   *
   * <p>The picture element is a container which provides multiple sources to its contained img
   * element to allow authors to declaratively control or give hints to the user agent about which
   * image resource to use, based on the screen pixel density, viewport size, image format, and other
   * factors.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/picture">MDN:
   * &lt;picture&gt;</a>
   */
  public static PictureTag picture() {
    return new PictureTag();
  }

  /**
   * Creates a <code>&lt;picture&gt;</code> element.
   *
   * <p>The picture element is a container which provides multiple sources to its contained img
   * element to allow authors to declaratively control or give hints to the user agent about which
   * image resource to use, based on the screen pixel density, viewport size, image format, and other
   * factors.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/picture">MDN:
   * &lt;picture&gt;</a>
   */
  public static PictureTag picture(String text) {
    return new PictureTag().withText(text);
  }

  /**
   * Creates a <code>&lt;picture&gt;</code> element.
   *
   * <p>The picture element is a container which provides multiple sources to its contained img
   * element to allow authors to declaratively control or give hints to the user agent about which
   * image resource to use, based on the screen pixel density, viewport size, image format, and other
   * factors.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/picture">MDN:
   * &lt;picture&gt;</a>
   */
  public static PictureTag picture(DomContent... children) {
    return new PictureTag().with(children);
  }

  /**
   * Creates a <code>&lt;pre&gt;</code> element.
   *
   * <p>The pre element represents a block of preformatted text, in which structure is represented by
   * typographic conventions rather than by elements.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/pre">MDN:
   * &lt;pre&gt;</a>
   */
  public static PreTag pre() {
    return new PreTag();
  }

  /**
   * Creates a <code>&lt;pre&gt;</code> element.
   *
   * <p>The pre element represents a block of preformatted text, in which structure is represented by
   * typographic conventions rather than by elements.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/pre">MDN:
   * &lt;pre&gt;</a>
   */
  public static PreTag pre(String text) {
    return new PreTag().withText(text);
  }

  /**
   * Creates a <code>&lt;pre&gt;</code> element.
   *
   * <p>The pre element represents a block of preformatted text, in which structure is represented by
   * typographic conventions rather than by elements.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/pre">MDN:
   * &lt;pre&gt;</a>
   */
  public static PreTag pre(DomContent... children) {
    return new PreTag().with(children);
  }

  /**
   * Creates a <code>&lt;progress&gt;</code> element.
   *
   * <p>The progress element represents the completion progress of a task.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/progress">MDN:
   * &lt;progress&gt;</a>
   */
  public static ProgressTag progress() {
    return new ProgressTag();
  }

  /**
   * Creates a <code>&lt;progress&gt;</code> element.
   *
   * <p>The progress element represents the completion progress of a task.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/progress">MDN:
   * &lt;progress&gt;</a>
   */
  public static ProgressTag progress(String text) {
    return new ProgressTag().withText(text);
  }

  /**
   * Creates a <code>&lt;progress&gt;</code> element.
   *
   * <p>The progress element represents the completion progress of a task.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/progress">MDN:
   * &lt;progress&gt;</a>
   */
  public static ProgressTag progress(DomContent... children) {
    return new ProgressTag().with(children);
  }

  /**
   * Creates a <code>&lt;q&gt;</code> element.
   *
   * <p>The q element represents some phrasing content quoted from another source.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/q">MDN:
   * &lt;q&gt;</a>
   */
  public static QTag q() {
    return new QTag();
  }

  /**
   * Creates a <code>&lt;q&gt;</code> element.
   *
   * <p>The q element represents some phrasing content quoted from another source.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/q">MDN:
   * &lt;q&gt;</a>
   */
  public static QTag q(String text) {
    return new QTag().withText(text);
  }

  /**
   * Creates a <code>&lt;q&gt;</code> element.
   *
   * <p>The q element represents some phrasing content quoted from another source.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/q">MDN:
   * &lt;q&gt;</a>
   */
  public static QTag q(DomContent... children) {
    return new QTag().with(children);
  }

  /**
   * Creates a <code>&lt;rb&gt;</code> element.
   *
   * <p>The rb element marks the base text component of a ruby annotation.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/rb">MDN:
   * &lt;rb&gt;</a>
   * @deprecated The <code>&lt;rb&gt;</code> element is marked deprecated in MDN browser-compat-data;
   * avoid in new markup.
   */
  @Deprecated
  public static RbTag rb() {
    return new RbTag();
  }

  /**
   * Creates a <code>&lt;rb&gt;</code> element.
   *
   * <p>The rb element marks the base text component of a ruby annotation.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/rb">MDN:
   * &lt;rb&gt;</a>
   * @deprecated The <code>&lt;rb&gt;</code> element is marked deprecated in MDN browser-compat-data;
   * avoid in new markup.
   */
  @Deprecated
  public static RbTag rb(String text) {
    return new RbTag().withText(text);
  }

  /**
   * Creates a <code>&lt;rb&gt;</code> element.
   *
   * <p>The rb element marks the base text component of a ruby annotation.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/rb">MDN:
   * &lt;rb&gt;</a>
   * @deprecated The <code>&lt;rb&gt;</code> element is marked deprecated in MDN browser-compat-data;
   * avoid in new markup.
   */
  @Deprecated
  public static RbTag rb(DomContent... children) {
    return new RbTag().with(children);
  }

  /**
   * Creates a <code>&lt;rp&gt;</code> element.
   *
   * <p>The rp element is used to provide fallback text to be shown by user agents that don't support
   * ruby annotations.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/rp">MDN:
   * &lt;rp&gt;</a>
   */
  public static RpTag rp() {
    return new RpTag();
  }

  /**
   * Creates a <code>&lt;rp&gt;</code> element.
   *
   * <p>The rp element is used to provide fallback text to be shown by user agents that don't support
   * ruby annotations.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/rp">MDN:
   * &lt;rp&gt;</a>
   */
  public static RpTag rp(String text) {
    return new RpTag().withText(text);
  }

  /**
   * Creates a <code>&lt;rp&gt;</code> element.
   *
   * <p>The rp element is used to provide fallback text to be shown by user agents that don't support
   * ruby annotations.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/rp">MDN:
   * &lt;rp&gt;</a>
   */
  public static RpTag rp(DomContent... children) {
    return new RpTag().with(children);
  }

  /**
   * Creates a <code>&lt;rt&gt;</code> element.
   *
   * <p>The rt element marks the ruby text component of a ruby annotation.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/rt">MDN:
   * &lt;rt&gt;</a>
   */
  public static RtTag rt() {
    return new RtTag();
  }

  /**
   * Creates a <code>&lt;rt&gt;</code> element.
   *
   * <p>The rt element marks the ruby text component of a ruby annotation.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/rt">MDN:
   * &lt;rt&gt;</a>
   */
  public static RtTag rt(String text) {
    return new RtTag().withText(text);
  }

  /**
   * Creates a <code>&lt;rt&gt;</code> element.
   *
   * <p>The rt element marks the ruby text component of a ruby annotation.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/rt">MDN:
   * &lt;rt&gt;</a>
   */
  public static RtTag rt(DomContent... children) {
    return new RtTag().with(children);
  }

  /**
   * Creates a <code>&lt;ruby&gt;</code> element.
   *
   * <p>The ruby element allows one or more spans of phrasing content to be marked with ruby
   * annotations.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/ruby">MDN:
   * &lt;ruby&gt;</a>
   */
  public static RubyTag ruby() {
    return new RubyTag();
  }

  /**
   * Creates a <code>&lt;ruby&gt;</code> element.
   *
   * <p>The ruby element allows one or more spans of phrasing content to be marked with ruby
   * annotations.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/ruby">MDN:
   * &lt;ruby&gt;</a>
   */
  public static RubyTag ruby(String text) {
    return new RubyTag().withText(text);
  }

  /**
   * Creates a <code>&lt;ruby&gt;</code> element.
   *
   * <p>The ruby element allows one or more spans of phrasing content to be marked with ruby
   * annotations.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/ruby">MDN:
   * &lt;ruby&gt;</a>
   */
  public static RubyTag ruby(DomContent... children) {
    return new RubyTag().with(children);
  }

  /**
   * Creates a <code>&lt;s&gt;</code> element.
   *
   * <p>The s element represents contents that are no longer accurate or no longer relevant.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/s">MDN:
   * &lt;s&gt;</a>
   */
  public static STag s() {
    return new STag();
  }

  /**
   * Creates a <code>&lt;s&gt;</code> element.
   *
   * <p>The s element represents contents that are no longer accurate or no longer relevant.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/s">MDN:
   * &lt;s&gt;</a>
   */
  public static STag s(String text) {
    return new STag().withText(text);
  }

  /**
   * Creates a <code>&lt;s&gt;</code> element.
   *
   * <p>The s element represents contents that are no longer accurate or no longer relevant.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/s">MDN:
   * &lt;s&gt;</a>
   */
  public static STag s(DomContent... children) {
    return new STag().with(children);
  }

  /**
   * Creates a <code>&lt;samp&gt;</code> element.
   *
   * <p>The samp element represents sample or quoted output from another program or computing system.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/samp">MDN:
   * &lt;samp&gt;</a>
   */
  public static SampTag samp() {
    return new SampTag();
  }

  /**
   * Creates a <code>&lt;samp&gt;</code> element.
   *
   * <p>The samp element represents sample or quoted output from another program or computing system.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/samp">MDN:
   * &lt;samp&gt;</a>
   */
  public static SampTag samp(String text) {
    return new SampTag().withText(text);
  }

  /**
   * Creates a <code>&lt;samp&gt;</code> element.
   *
   * <p>The samp element represents sample or quoted output from another program or computing system.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/samp">MDN:
   * &lt;samp&gt;</a>
   */
  public static SampTag samp(DomContent... children) {
    return new SampTag().with(children);
  }

  /**
   * Creates a <code>&lt;script&gt;</code> element.
   *
   * <p>The script element allows authors to include dynamic script and data blocks in their
   * documents.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/script">MDN:
   * &lt;script&gt;</a>
   */
  public static ScriptTag script() {
    return new ScriptTag();
  }

  /**
   * Creates a <code>&lt;script&gt;</code> element.
   *
   * <p>The script element allows authors to include dynamic script and data blocks in their
   * documents.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/script">MDN:
   * &lt;script&gt;</a>
   */
  public static ScriptTag script(String text) {
    return new ScriptTag().withText(text);
  }

  /**
   * Creates a <code>&lt;script&gt;</code> element.
   *
   * <p>The script element allows authors to include dynamic script and data blocks in their
   * documents.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/script">MDN:
   * &lt;script&gt;</a>
   */
  public static ScriptTag script(DomContent... children) {
    return new ScriptTag().with(children);
  }

  /**
   * Creates a <code>&lt;search&gt;</code> element.
   *
   * <p>The search element represents the parts of the document or application with form controls or
   * other content related to performing a search or filtering operation.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/search">MDN:
   * &lt;search&gt;</a>
   */
  public static SearchTag search() {
    return new SearchTag();
  }

  /**
   * Creates a <code>&lt;search&gt;</code> element.
   *
   * <p>The search element represents the parts of the document or application with form controls or
   * other content related to performing a search or filtering operation.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/search">MDN:
   * &lt;search&gt;</a>
   */
  public static SearchTag search(String text) {
    return new SearchTag().withText(text);
  }

  /**
   * Creates a <code>&lt;search&gt;</code> element.
   *
   * <p>The search element represents the parts of the document or application with form controls or
   * other content related to performing a search or filtering operation.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/search">MDN:
   * &lt;search&gt;</a>
   */
  public static SearchTag search(DomContent... children) {
    return new SearchTag().with(children);
  }

  /**
   * Creates a <code>&lt;section&gt;</code> element.
   *
   * <p>The section element represents a generic section of a document or application.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/section">MDN:
   * &lt;section&gt;</a>
   */
  public static SectionTag section() {
    return new SectionTag();
  }

  /**
   * Creates a <code>&lt;section&gt;</code> element.
   *
   * <p>The section element represents a generic section of a document or application.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/section">MDN:
   * &lt;section&gt;</a>
   */
  public static SectionTag section(String text) {
    return new SectionTag().withText(text);
  }

  /**
   * Creates a <code>&lt;section&gt;</code> element.
   *
   * <p>The section element represents a generic section of a document or application.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/section">MDN:
   * &lt;section&gt;</a>
   */
  public static SectionTag section(DomContent... children) {
    return new SectionTag().with(children);
  }

  /**
   * Creates a <code>&lt;select&gt;</code> element.
   *
   * <p>The select element represents a control for selecting amongst a set of options.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/select">MDN:
   * &lt;select&gt;</a>
   */
  public static SelectTag select() {
    return new SelectTag();
  }

  /**
   * Creates a <code>&lt;select&gt;</code> element.
   *
   * <p>The select element represents a control for selecting amongst a set of options.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/select">MDN:
   * &lt;select&gt;</a>
   */
  public static SelectTag select(String text) {
    return new SelectTag().withText(text);
  }

  /**
   * Creates a <code>&lt;select&gt;</code> element.
   *
   * <p>The select element represents a control for selecting amongst a set of options.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/select">MDN:
   * &lt;select&gt;</a>
   */
  public static SelectTag select(DomContent... children) {
    return new SelectTag().with(children);
  }

  /**
   * Creates a <code>&lt;selectedcontent&gt;</code> element.
   *
   * <p>The selectedcontent element can be used to display the content of the currently selected
   * option element inside of a closed select element.
   *
   * @see <a
   * href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/selectedcontent">MDN:
   * &lt;selectedcontent&gt;</a>
   */
  public static SelectedcontentTag selectedcontent() {
    return new SelectedcontentTag();
  }

  /**
   * Creates a <code>&lt;selectedcontent&gt;</code> element.
   *
   * <p>The selectedcontent element can be used to display the content of the currently selected
   * option element inside of a closed select element.
   *
   * @see <a
   * href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/selectedcontent">MDN:
   * &lt;selectedcontent&gt;</a>
   */
  public static SelectedcontentTag selectedcontent(String text) {
    return new SelectedcontentTag().withText(text);
  }

  /**
   * Creates a <code>&lt;selectedcontent&gt;</code> element.
   *
   * <p>The selectedcontent element can be used to display the content of the currently selected
   * option element inside of a closed select element.
   *
   * @see <a
   * href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/selectedcontent">MDN:
   * &lt;selectedcontent&gt;</a>
   */
  public static SelectedcontentTag selectedcontent(DomContent... children) {
    return new SelectedcontentTag().with(children);
  }

  /**
   * Creates a <code>&lt;slot&gt;</code> element.
   *
   * <p>The slot element is a placeholder inside a web component that you can fill with your own
   * markup, which lets you create separate DOM trees and present them together.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/slot">MDN:
   * &lt;slot&gt;</a>
   */
  public static SlotTag slot() {
    return new SlotTag();
  }

  /**
   * Creates a <code>&lt;slot&gt;</code> element.
   *
   * <p>The slot element is a placeholder inside a web component that you can fill with your own
   * markup, which lets you create separate DOM trees and present them together.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/slot">MDN:
   * &lt;slot&gt;</a>
   */
  public static SlotTag slot(String text) {
    return new SlotTag().withText(text);
  }

  /**
   * Creates a <code>&lt;slot&gt;</code> element.
   *
   * <p>The slot element is a placeholder inside a web component that you can fill with your own
   * markup, which lets you create separate DOM trees and present them together.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/slot">MDN:
   * &lt;slot&gt;</a>
   */
  public static SlotTag slot(DomContent... children) {
    return new SlotTag().with(children);
  }

  /**
   * Creates a <code>&lt;small&gt;</code> element.
   *
   * <p>The small element represents side comments such as small print.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/small">MDN:
   * &lt;small&gt;</a>
   */
  public static SmallTag small() {
    return new SmallTag();
  }

  /**
   * Creates a <code>&lt;small&gt;</code> element.
   *
   * <p>The small element represents side comments such as small print.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/small">MDN:
   * &lt;small&gt;</a>
   */
  public static SmallTag small(String text) {
    return new SmallTag().withText(text);
  }

  /**
   * Creates a <code>&lt;small&gt;</code> element.
   *
   * <p>The small element represents side comments such as small print.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/small">MDN:
   * &lt;small&gt;</a>
   */
  public static SmallTag small(DomContent... children) {
    return new SmallTag().with(children);
  }

  /**
   * Creates a <code>&lt;source&gt;</code> element.
   *
   * <p>The source element allows authors to specify multiple alternative media resources for media
   * elements.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/source">MDN:
   * &lt;source&gt;</a>
   */
  public static SourceTag source() {
    return new SourceTag();
  }

  /**
   * Creates a <code>&lt;span&gt;</code> element.
   *
   * <p>The span element doesn't mean anything on its own, but can be useful when used together with
   * the global attributes, e.g.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/span">MDN:
   * &lt;span&gt;</a>
   */
  public static SpanTag span() {
    return new SpanTag();
  }

  /**
   * Creates a <code>&lt;span&gt;</code> element.
   *
   * <p>The span element doesn't mean anything on its own, but can be useful when used together with
   * the global attributes, e.g.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/span">MDN:
   * &lt;span&gt;</a>
   */
  public static SpanTag span(String text) {
    return new SpanTag().withText(text);
  }

  /**
   * Creates a <code>&lt;span&gt;</code> element.
   *
   * <p>The span element doesn't mean anything on its own, but can be useful when used together with
   * the global attributes, e.g.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/span">MDN:
   * &lt;span&gt;</a>
   */
  public static SpanTag span(DomContent... children) {
    return new SpanTag().with(children);
  }

  /**
   * Creates a <code>&lt;strong&gt;</code> element.
   *
   * <p>The strong element represents strong importance, seriousness, or urgency for its contents.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/strong">MDN:
   * &lt;strong&gt;</a>
   */
  public static StrongTag strong() {
    return new StrongTag();
  }

  /**
   * Creates a <code>&lt;strong&gt;</code> element.
   *
   * <p>The strong element represents strong importance, seriousness, or urgency for its contents.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/strong">MDN:
   * &lt;strong&gt;</a>
   */
  public static StrongTag strong(String text) {
    return new StrongTag().withText(text);
  }

  /**
   * Creates a <code>&lt;strong&gt;</code> element.
   *
   * <p>The strong element represents strong importance, seriousness, or urgency for its contents.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/strong">MDN:
   * &lt;strong&gt;</a>
   */
  public static StrongTag strong(DomContent... children) {
    return new StrongTag().with(children);
  }

  /**
   * Creates a <code>&lt;style&gt;</code> element.
   *
   * <p>The style element allows authors to embed style information in their documents.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/style">MDN:
   * &lt;style&gt;</a>
   */
  public static StyleTag style() {
    return new StyleTag();
  }

  /**
   * Creates a <code>&lt;style&gt;</code> element.
   *
   * <p>The style element allows authors to embed style information in their documents.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/style">MDN:
   * &lt;style&gt;</a>
   */
  public static StyleTag style(String text) {
    return new StyleTag().withText(text);
  }

  /**
   * Creates a <code>&lt;style&gt;</code> element.
   *
   * <p>The style element allows authors to embed style information in their documents.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/style">MDN:
   * &lt;style&gt;</a>
   */
  public static StyleTag style(DomContent... children) {
    return new StyleTag().with(children);
  }

  /**
   * Creates a <code>&lt;sub&gt;</code> element.
   *
   * <p>The sub element represents a subscript.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/sub">MDN:
   * &lt;sub&gt;</a>
   */
  public static SubTag sub() {
    return new SubTag();
  }

  /**
   * Creates a <code>&lt;sub&gt;</code> element.
   *
   * <p>The sub element represents a subscript.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/sub">MDN:
   * &lt;sub&gt;</a>
   */
  public static SubTag sub(String text) {
    return new SubTag().withText(text);
  }

  /**
   * Creates a <code>&lt;sub&gt;</code> element.
   *
   * <p>The sub element represents a subscript.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/sub">MDN:
   * &lt;sub&gt;</a>
   */
  public static SubTag sub(DomContent... children) {
    return new SubTag().with(children);
  }

  /**
   * Creates a <code>&lt;summary&gt;</code> element.
   *
   * <p>The summary element represents a summary, caption, or legend for the rest of the contents of
   * the summary element's parent details element, if any.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/summary">MDN:
   * &lt;summary&gt;</a>
   */
  public static SummaryTag summary() {
    return new SummaryTag();
  }

  /**
   * Creates a <code>&lt;summary&gt;</code> element.
   *
   * <p>The summary element represents a summary, caption, or legend for the rest of the contents of
   * the summary element's parent details element, if any.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/summary">MDN:
   * &lt;summary&gt;</a>
   */
  public static SummaryTag summary(String text) {
    return new SummaryTag().withText(text);
  }

  /**
   * Creates a <code>&lt;summary&gt;</code> element.
   *
   * <p>The summary element represents a summary, caption, or legend for the rest of the contents of
   * the summary element's parent details element, if any.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/summary">MDN:
   * &lt;summary&gt;</a>
   */
  public static SummaryTag summary(DomContent... children) {
    return new SummaryTag().with(children);
  }

  /**
   * Creates a <code>&lt;sup&gt;</code> element.
   *
   * <p>The sup element represents a superscript.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/sup">MDN:
   * &lt;sup&gt;</a>
   */
  public static SupTag sup() {
    return new SupTag();
  }

  /**
   * Creates a <code>&lt;sup&gt;</code> element.
   *
   * <p>The sup element represents a superscript.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/sup">MDN:
   * &lt;sup&gt;</a>
   */
  public static SupTag sup(String text) {
    return new SupTag().withText(text);
  }

  /**
   * Creates a <code>&lt;sup&gt;</code> element.
   *
   * <p>The sup element represents a superscript.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/sup">MDN:
   * &lt;sup&gt;</a>
   */
  public static SupTag sup(DomContent... children) {
    return new SupTag().with(children);
  }

  /**
   * Creates a <code>&lt;table&gt;</code> element.
   *
   * <p>The table element represents data with more than one dimension, in the form of a table.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/table">MDN:
   * &lt;table&gt;</a>
   */
  public static TableTag table() {
    return new TableTag();
  }

  /**
   * Creates a <code>&lt;table&gt;</code> element.
   *
   * <p>The table element represents data with more than one dimension, in the form of a table.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/table">MDN:
   * &lt;table&gt;</a>
   */
  public static TableTag table(String text) {
    return new TableTag().withText(text);
  }

  /**
   * Creates a <code>&lt;table&gt;</code> element.
   *
   * <p>The table element represents data with more than one dimension, in the form of a table.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/table">MDN:
   * &lt;table&gt;</a>
   */
  public static TableTag table(DomContent... children) {
    return new TableTag().with(children);
  }

  /**
   * Creates a <code>&lt;tbody&gt;</code> element.
   *
   * <p>The tbody element represents a block of rows that consist of a body of data for the parent
   * table element, if the tbody element has a parent and it is a table.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/tbody">MDN:
   * &lt;tbody&gt;</a>
   */
  public static TbodyTag tbody() {
    return new TbodyTag();
  }

  /**
   * Creates a <code>&lt;tbody&gt;</code> element.
   *
   * <p>The tbody element represents a block of rows that consist of a body of data for the parent
   * table element, if the tbody element has a parent and it is a table.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/tbody">MDN:
   * &lt;tbody&gt;</a>
   */
  public static TbodyTag tbody(String text) {
    return new TbodyTag().withText(text);
  }

  /**
   * Creates a <code>&lt;tbody&gt;</code> element.
   *
   * <p>The tbody element represents a block of rows that consist of a body of data for the parent
   * table element, if the tbody element has a parent and it is a table.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/tbody">MDN:
   * &lt;tbody&gt;</a>
   */
  public static TbodyTag tbody(DomContent... children) {
    return new TbodyTag().with(children);
  }

  /**
   * Creates a <code>&lt;td&gt;</code> element.
   *
   * <p>The td element represents a data cell in a table.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/td">MDN:
   * &lt;td&gt;</a>
   */
  public static TdTag td() {
    return new TdTag();
  }

  /**
   * Creates a <code>&lt;td&gt;</code> element.
   *
   * <p>The td element represents a data cell in a table.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/td">MDN:
   * &lt;td&gt;</a>
   */
  public static TdTag td(String text) {
    return new TdTag().withText(text);
  }

  /**
   * Creates a <code>&lt;td&gt;</code> element.
   *
   * <p>The td element represents a data cell in a table.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/td">MDN:
   * &lt;td&gt;</a>
   */
  public static TdTag td(DomContent... children) {
    return new TdTag().with(children);
  }

  /**
   * Creates a <code>&lt;template&gt;</code> element.
   *
   * <p>The template element is used to declare fragments of HTML that can be cloned and inserted in
   * the document by script.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/template">MDN:
   * &lt;template&gt;</a>
   */
  public static TemplateTag template() {
    return new TemplateTag();
  }

  /**
   * Creates a <code>&lt;template&gt;</code> element.
   *
   * <p>The template element is used to declare fragments of HTML that can be cloned and inserted in
   * the document by script.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/template">MDN:
   * &lt;template&gt;</a>
   */
  public static TemplateTag template(String text) {
    return new TemplateTag().withText(text);
  }

  /**
   * Creates a <code>&lt;template&gt;</code> element.
   *
   * <p>The template element is used to declare fragments of HTML that can be cloned and inserted in
   * the document by script.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/template">MDN:
   * &lt;template&gt;</a>
   */
  public static TemplateTag template(DomContent... children) {
    return new TemplateTag().with(children);
  }

  /**
   * Creates a <code>&lt;textarea&gt;</code> element.
   *
   * <p>The textarea element represents a multiline plain text edit control for the element's raw
   * value.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/textarea">MDN:
   * &lt;textarea&gt;</a>
   */
  public static TextareaTag textarea() {
    return new TextareaTag();
  }

  /**
   * Creates a <code>&lt;textarea&gt;</code> element.
   *
   * <p>The textarea element represents a multiline plain text edit control for the element's raw
   * value.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/textarea">MDN:
   * &lt;textarea&gt;</a>
   */
  public static TextareaTag textarea(String text) {
    return new TextareaTag().withText(text);
  }

  /**
   * Creates a <code>&lt;textarea&gt;</code> element.
   *
   * <p>The textarea element represents a multiline plain text edit control for the element's raw
   * value.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/textarea">MDN:
   * &lt;textarea&gt;</a>
   */
  public static TextareaTag textarea(DomContent... children) {
    return new TextareaTag().with(children);
  }

  /**
   * Creates a <code>&lt;tfoot&gt;</code> element.
   *
   * <p>The tfoot element represents the block of rows that consist of the column summaries (footers)
   * for the parent table element, if the tfoot element has a parent and it is a table.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/tfoot">MDN:
   * &lt;tfoot&gt;</a>
   */
  public static TfootTag tfoot() {
    return new TfootTag();
  }

  /**
   * Creates a <code>&lt;tfoot&gt;</code> element.
   *
   * <p>The tfoot element represents the block of rows that consist of the column summaries (footers)
   * for the parent table element, if the tfoot element has a parent and it is a table.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/tfoot">MDN:
   * &lt;tfoot&gt;</a>
   */
  public static TfootTag tfoot(String text) {
    return new TfootTag().withText(text);
  }

  /**
   * Creates a <code>&lt;tfoot&gt;</code> element.
   *
   * <p>The tfoot element represents the block of rows that consist of the column summaries (footers)
   * for the parent table element, if the tfoot element has a parent and it is a table.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/tfoot">MDN:
   * &lt;tfoot&gt;</a>
   */
  public static TfootTag tfoot(DomContent... children) {
    return new TfootTag().with(children);
  }

  /**
   * Creates a <code>&lt;th&gt;</code> element.
   *
   * <p>The th element represents a header cell in a table.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/th">MDN:
   * &lt;th&gt;</a>
   */
  public static ThTag th() {
    return new ThTag();
  }

  /**
   * Creates a <code>&lt;th&gt;</code> element.
   *
   * <p>The th element represents a header cell in a table.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/th">MDN:
   * &lt;th&gt;</a>
   */
  public static ThTag th(String text) {
    return new ThTag().withText(text);
  }

  /**
   * Creates a <code>&lt;th&gt;</code> element.
   *
   * <p>The th element represents a header cell in a table.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/th">MDN:
   * &lt;th&gt;</a>
   */
  public static ThTag th(DomContent... children) {
    return new ThTag().with(children);
  }

  /**
   * Creates a <code>&lt;thead&gt;</code> element.
   *
   * <p>The thead element represents the block of rows that consist of the column labels (headers)
   * for the parent table element, if the thead element has a parent and it is a table.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/thead">MDN:
   * &lt;thead&gt;</a>
   */
  public static TheadTag thead() {
    return new TheadTag();
  }

  /**
   * Creates a <code>&lt;thead&gt;</code> element.
   *
   * <p>The thead element represents the block of rows that consist of the column labels (headers)
   * for the parent table element, if the thead element has a parent and it is a table.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/thead">MDN:
   * &lt;thead&gt;</a>
   */
  public static TheadTag thead(String text) {
    return new TheadTag().withText(text);
  }

  /**
   * Creates a <code>&lt;thead&gt;</code> element.
   *
   * <p>The thead element represents the block of rows that consist of the column labels (headers)
   * for the parent table element, if the thead element has a parent and it is a table.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/thead">MDN:
   * &lt;thead&gt;</a>
   */
  public static TheadTag thead(DomContent... children) {
    return new TheadTag().with(children);
  }

  /**
   * Creates a <code>&lt;time&gt;</code> element.
   *
   * <p>The time element represents its contents, along with a machine-readable form of those
   * contents in the datetime attribute.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/time">MDN:
   * &lt;time&gt;</a>
   */
  public static TimeTag time() {
    return new TimeTag();
  }

  /**
   * Creates a <code>&lt;time&gt;</code> element.
   *
   * <p>The time element represents its contents, along with a machine-readable form of those
   * contents in the datetime attribute.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/time">MDN:
   * &lt;time&gt;</a>
   */
  public static TimeTag time(String text) {
    return new TimeTag().withText(text);
  }

  /**
   * Creates a <code>&lt;time&gt;</code> element.
   *
   * <p>The time element represents its contents, along with a machine-readable form of those
   * contents in the datetime attribute.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/time">MDN:
   * &lt;time&gt;</a>
   */
  public static TimeTag time(DomContent... children) {
    return new TimeTag().with(children);
  }

  /**
   * Creates a <code>&lt;title&gt;</code> element.
   *
   * <p>The title element represents the document's title or name.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/title">MDN:
   * &lt;title&gt;</a>
   */
  public static TitleTag title() {
    return new TitleTag();
  }

  /**
   * Creates a <code>&lt;title&gt;</code> element.
   *
   * <p>The title element represents the document's title or name.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/title">MDN:
   * &lt;title&gt;</a>
   */
  public static TitleTag title(String text) {
    return new TitleTag().withText(text);
  }

  /**
   * Creates a <code>&lt;title&gt;</code> element.
   *
   * <p>The title element represents the document's title or name.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/title">MDN:
   * &lt;title&gt;</a>
   */
  public static TitleTag title(DomContent... children) {
    return new TitleTag().with(children);
  }

  /**
   * Creates a <code>&lt;tr&gt;</code> element.
   *
   * <p>The tr element represents a row of cells in a table.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/tr">MDN:
   * &lt;tr&gt;</a>
   */
  public static TrTag tr() {
    return new TrTag();
  }

  /**
   * Creates a <code>&lt;tr&gt;</code> element.
   *
   * <p>The tr element represents a row of cells in a table.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/tr">MDN:
   * &lt;tr&gt;</a>
   */
  public static TrTag tr(String text) {
    return new TrTag().withText(text);
  }

  /**
   * Creates a <code>&lt;tr&gt;</code> element.
   *
   * <p>The tr element represents a row of cells in a table.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/tr">MDN:
   * &lt;tr&gt;</a>
   */
  public static TrTag tr(DomContent... children) {
    return new TrTag().with(children);
  }

  /**
   * Creates a <code>&lt;track&gt;</code> element.
   *
   * <p>The track element allows authors to specify explicit external timed text tracks for media
   * elements.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/track">MDN:
   * &lt;track&gt;</a>
   */
  public static TrackTag track() {
    return new TrackTag();
  }

  /**
   * Creates a <code>&lt;u&gt;</code> element.
   *
   * <p>The u element represents a span of text with an unarticulated, though explicitly rendered,
   * non-textual annotation, such as labeling the text as being a proper name in Chinese text (a
   * Chinese proper name mark), or labeling the text as being misspelt.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/u">MDN:
   * &lt;u&gt;</a>
   */
  public static UTag u() {
    return new UTag();
  }

  /**
   * Creates a <code>&lt;u&gt;</code> element.
   *
   * <p>The u element represents a span of text with an unarticulated, though explicitly rendered,
   * non-textual annotation, such as labeling the text as being a proper name in Chinese text (a
   * Chinese proper name mark), or labeling the text as being misspelt.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/u">MDN:
   * &lt;u&gt;</a>
   */
  public static UTag u(String text) {
    return new UTag().withText(text);
  }

  /**
   * Creates a <code>&lt;u&gt;</code> element.
   *
   * <p>The u element represents a span of text with an unarticulated, though explicitly rendered,
   * non-textual annotation, such as labeling the text as being a proper name in Chinese text (a
   * Chinese proper name mark), or labeling the text as being misspelt.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/u">MDN:
   * &lt;u&gt;</a>
   */
  public static UTag u(DomContent... children) {
    return new UTag().with(children);
  }

  /**
   * Creates a <code>&lt;ul&gt;</code> element.
   *
   * <p>The ul element represents a list of items, where the order of the items is not important —
   * that is, where changing the order would not materially change the meaning of the document.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/ul">MDN:
   * &lt;ul&gt;</a>
   */
  public static UlTag ul() {
    return new UlTag();
  }

  /**
   * Creates a <code>&lt;ul&gt;</code> element.
   *
   * <p>The ul element represents a list of items, where the order of the items is not important —
   * that is, where changing the order would not materially change the meaning of the document.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/ul">MDN:
   * &lt;ul&gt;</a>
   */
  public static UlTag ul(String text) {
    return new UlTag().withText(text);
  }

  /**
   * Creates a <code>&lt;ul&gt;</code> element.
   *
   * <p>The ul element represents a list of items, where the order of the items is not important —
   * that is, where changing the order would not materially change the meaning of the document.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/ul">MDN:
   * &lt;ul&gt;</a>
   */
  public static UlTag ul(DomContent... children) {
    return new UlTag().with(children);
  }

  /**
   * Creates a <code>&lt;var&gt;</code> element.
   *
   * <p>The var element represents a variable.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/var">MDN:
   * &lt;var&gt;</a>
   */
  public static VarTag var() {
    return new VarTag();
  }

  /**
   * Creates a <code>&lt;var&gt;</code> element.
   *
   * <p>The var element represents a variable.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/var">MDN:
   * &lt;var&gt;</a>
   */
  public static VarTag var(String text) {
    return new VarTag().withText(text);
  }

  /**
   * Creates a <code>&lt;var&gt;</code> element.
   *
   * <p>The var element represents a variable.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/var">MDN:
   * &lt;var&gt;</a>
   */
  public static VarTag var(DomContent... children) {
    return new VarTag().with(children);
  }

  /**
   * Creates a <code>&lt;video&gt;</code> element.
   *
   * <p>A video element is used for playing videos or movies, and audio files with captions.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/video">MDN:
   * &lt;video&gt;</a>
   */
  public static VideoTag video() {
    return new VideoTag();
  }

  /**
   * Creates a <code>&lt;video&gt;</code> element.
   *
   * <p>A video element is used for playing videos or movies, and audio files with captions.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/video">MDN:
   * &lt;video&gt;</a>
   */
  public static VideoTag video(String text) {
    return new VideoTag().withText(text);
  }

  /**
   * Creates a <code>&lt;video&gt;</code> element.
   *
   * <p>A video element is used for playing videos or movies, and audio files with captions.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/video">MDN:
   * &lt;video&gt;</a>
   */
  public static VideoTag video(DomContent... children) {
    return new VideoTag().with(children);
  }

  /**
   * Creates a <code>&lt;wbr&gt;</code> element.
   *
   * <p>The wbr element represents a line break opportunity.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/wbr">MDN:
   * &lt;wbr&gt;</a>
   */
  public static WbrTag wbr() {
    return new WbrTag();
  }
}
