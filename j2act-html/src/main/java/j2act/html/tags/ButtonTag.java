// Generated by j2act-codegen. Do not edit; regenerate with:
//   ./mvnw -pl j2act-codegen compile exec:java
// Data: @vscode/web-custom-data (MIT), @mdn/browser-compat-data (CC0).
// API shape follows j2html (Apache-2.0). See NOTICE.
package j2act.html.tags;

import j2act.ContainerTag;

/**
 * The <code>&lt;button&gt;</code> element.
 *
 * <p>The button element represents a button labeled by its contents.
 *
 * <p>Baseline: widely available since 2018-01-29.
 *
 * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/button">MDN:
 * &lt;button&gt;</a>
 */
public final class ButtonTag extends ContainerTag<ButtonTag> {

  public ButtonTag() {
    super("button");
  }

  /**
   * Sets the <code>disabled</code> attribute.
   *
   * <p>This Boolean attribute indicates that the user cannot interact with the button. If this
   * attribute is not specified, the button inherits its setting from the containing element, for
   * example
   * [<code>&lt;fieldset&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/fieldset
   * "The HTML &lt;fieldset&gt; element is used to group several controls as well as labels
   * (&lt;label&gt;) within a web form."); if there is no containing element with the **disabled**
   * attribute set, then the button is enabled.
   *
   * <p>Firefox will, unlike other browsers, by default, <a
   * href="https://stackoverflow.com/questions/5985839/bug-with-firefox-disabled-attribute-of-input-not-resetting-when-refreshing">persist
   * the dynamic disabled state</a> of a
   * [<code>&lt;button&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button
   * "The HTML &lt;button&gt; element represents a clickable button, which can be used in forms or
   * anywhere in a document that needs simple, standard button functionality.") across page loads.
   * Use the <a
   * href="https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button#attr-autocomplete"><code>autocomplete</code></a>
   * attribute to control this feature.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Attributes/disabled">MDN:
   * disabled</a>
   */
  public ButtonTag isDisabled() {
    return attr("disabled");
  }

  /**
   * Applies {@link #isDisabled()} when {@code condition} is true and removes the attribute
   * otherwise, so a form control stays controlled either way (ADR 0013).
   */
  public ButtonTag withCondDisabled(boolean condition) {
    return condition ? attr("disabled") : attr("disabled", null);
  }

  /**
   * Sets the <code>form</code> attribute.
   *
   * <p>The form element that the button is associated with (its _form owner_). The value of the
   * attribute must be the **id** attribute of a
   * [<code>&lt;form&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/form "The
   * HTML &lt;form&gt; element represents a document section that contains interactive controls for
   * submitting information to a web server.") element in the same document. If this attribute is not
   * specified, the <code>&lt;button&gt;</code> element will be associated to an ancestor
   * [<code>&lt;form&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/form "The
   * HTML &lt;form&gt; element represents a document section that contains interactive controls for
   * submitting information to a web server.") element, if one exists. This attribute enables you to
   * associate <code>&lt;button&gt;</code> elements to
   * [<code>&lt;form&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/form "The
   * HTML &lt;form&gt; element represents a document section that contains interactive controls for
   * submitting information to a web server.") elements anywhere within a document, not just as
   * descendants of
   * [<code>&lt;form&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/form "The
   * HTML &lt;form&gt; element represents a document section that contains interactive controls for
   * submitting information to a web server.") elements.
   *
   * <p>Baseline: widely available since 2020-04-17.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Attributes/form">MDN:
   * form</a>
   */
  public ButtonTag withForm(String value) {
    return attr("form", value);
  }

  /**
   * Applies {@link #withForm(String)} only when {@code condition} is true.
   */
  public ButtonTag withCondForm(boolean condition, String value) {
    return condition ? attr("form", value) : self();
  }

  /**
   * Sets the <code>formaction</code> attribute.
   *
   * <p>The URI of a program that processes the information submitted by the button. If specified, it
   * overrides the <a
   * href="https://developer.mozilla.org/en-US/docs/Web/HTML/Element/form#attr-action"><code>action</code></a>
   * attribute of the button's form owner.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/button">MDN:
   * formaction</a>
   */
  public ButtonTag withFormaction(String value) {
    return attr("formaction", value);
  }

  /**
   * Applies {@link #withFormaction(String)} only when {@code condition} is true.
   */
  public ButtonTag withCondFormaction(boolean condition, String value) {
    return condition ? attr("formaction", value) : self();
  }

  /**
   * Sets the <code>formenctype</code> attribute.
   *
   * <p>If the button is a submit button, this attribute specifies the type of content that is used
   * to submit the form to the server. Possible values are:
   *
   * <p>* <code>application/x-www-form-urlencoded</code>: The default value if the attribute is not
   * specified. * <code>multipart/form-data</code>: Use this value if you are using an
   * [<code>&lt;input&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input
   * "The HTML &lt;input&gt; element is used to create interactive controls for web-based forms in
   * order to accept data from the user; a wide variety of types of input data and control widgets
   * are available, depending on the device and user agent.") element with the <a
   * href="https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#attr-type"><code>type</code></a>
   * attribute set to <code>file</code>. * <code>text/plain</code>
   *
   * <p>If this attribute is specified, it overrides the <a
   * href="https://developer.mozilla.org/en-US/docs/Web/HTML/Element/form#attr-enctype"><code>enctype</code></a>
   * attribute of the button's form owner.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/button">MDN:
   * formenctype</a>
   */
  public ButtonTag withFormenctype(String value) {
    return attr("formenctype", value);
  }

  /**
   * Applies {@link #withFormenctype(String)} only when {@code condition} is true.
   */
  public ButtonTag withCondFormenctype(boolean condition, String value) {
    return condition ? attr("formenctype", value) : self();
  }

  /**
   * Sets the <code>formmethod</code> attribute.
   *
   * <p>If the button is a submit button, this attribute specifies the HTTP method that the browser
   * uses to submit the form. Possible values are:
   *
   * <p>* <code>post</code>: The data from the form are included in the body of the form and sent to
   * the server. * <code>get</code>: The data from the form are appended to the **form** attribute
   * URI, with a '?' as a separator, and the resulting URI is sent to the server. Use this method
   * when the form has no side-effects and contains only ASCII characters.
   *
   * <p>If specified, this attribute overrides the <a
   * href="https://developer.mozilla.org/en-US/docs/Web/HTML/Element/form#attr-method"><code>method</code></a>
   * attribute of the button's form owner.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/button">MDN:
   * formmethod</a>
   */
  public ButtonTag withFormmethod(String value) {
    return attr("formmethod", value);
  }

  /**
   * Applies {@link #withFormmethod(String)} only when {@code condition} is true.
   */
  public ButtonTag withCondFormmethod(boolean condition, String value) {
    return condition ? attr("formmethod", value) : self();
  }

  /**
   * Sets the <code>formnovalidate</code> attribute.
   *
   * <p>If the button is a submit button, this Boolean attribute specifies that the form is not to be
   * validated when it is submitted. If this attribute is specified, it overrides the <a
   * href="https://developer.mozilla.org/en-US/docs/Web/HTML/Element/form#attr-novalidate"><code>novalidate</code></a>
   * attribute of the button's form owner.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/button">MDN:
   * formnovalidate</a>
   */
  public ButtonTag isFormnovalidate() {
    return attr("formnovalidate");
  }

  /**
   * Applies {@link #isFormnovalidate()} when {@code condition} is true and removes the attribute
   * otherwise, so a form control stays controlled either way (ADR 0013).
   */
  public ButtonTag withCondFormnovalidate(boolean condition) {
    return condition ? attr("formnovalidate") : attr("formnovalidate", null);
  }

  /**
   * Sets the <code>formtarget</code> attribute.
   *
   * <p>If the button is a submit button, this attribute is a name or keyword indicating where to
   * display the response that is received after submitting the form. This is a name of, or keyword
   * for, a _browsing context_ (for example, tab, window, or inline frame). If this attribute is
   * specified, it overrides the <a
   * href="https://developer.mozilla.org/en-US/docs/Web/HTML/Element/form#attr-target"><code>target</code></a>
   * attribute of the button's form owner. The following keywords have special meanings:
   *
   * <p>* <code>_self</code>: Load the response into the same browsing context as the current one.
   * This value is the default if the attribute is not specified. * <code>_blank</code>: Load the
   * response into a new unnamed browsing context. * <code>_parent</code>: Load the response into the
   * parent browsing context of the current one. If there is no parent, this option behaves the same
   * way as <code>_self</code>. * <code>_top</code>: Load the response into the top-level browsing
   * context (that is, the browsing context that is an ancestor of the current one, and has no
   * parent). If there is no parent, this option behaves the same way as <code>_self</code>.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/button">MDN:
   * formtarget</a>
   */
  public ButtonTag withFormtarget(String value) {
    return attr("formtarget", value);
  }

  /**
   * Applies {@link #withFormtarget(String)} only when {@code condition} is true.
   */
  public ButtonTag withCondFormtarget(boolean condition, String value) {
    return condition ? attr("formtarget", value) : self();
  }

  /**
   * Sets the <code>name</code> attribute.
   *
   * <p>The name of the button, which is submitted with the form data.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/button">MDN:
   * name</a>
   */
  public ButtonTag withName(String value) {
    return attr("name", value);
  }

  /**
   * Applies {@link #withName(String)} only when {@code condition} is true.
   */
  public ButtonTag withCondName(boolean condition, String value) {
    return condition ? attr("name", value) : self();
  }

  /**
   * Sets the <code>popovertarget</code> attribute.
   *
   * <p>Turns the button into a popover control button; takes the ID of the popover element to
   * control as its value.
   *
   * <p>Baseline: newly available since 2024-04-16.
   *
   * @see <a
   * href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/button#popovertarget">MDN:
   * popovertarget</a>
   */
  public ButtonTag withPopovertarget(String value) {
    return attr("popovertarget", value);
  }

  /**
   * Applies {@link #withPopovertarget(String)} only when {@code condition} is true.
   */
  public ButtonTag withCondPopovertarget(boolean condition, String value) {
    return condition ? attr("popovertarget", value) : self();
  }

  /**
   * Sets the <code>popovertargetaction</code> attribute.
   *
   * <p>Specifies the action to be performed on a popover element being controlled by the button.
   *
   * <p>Baseline: newly available since 2024-04-16.
   *
   * @see <a
   * href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/button#popovertargetaction">MDN:
   * popovertargetaction</a>
   */
  public ButtonTag withPopovertargetaction(String value) {
    return attr("popovertargetaction", value);
  }

  /**
   * Applies {@link #withPopovertargetaction(String)} only when {@code condition} is true.
   */
  public ButtonTag withCondPopovertargetaction(boolean condition, String value) {
    return condition ? attr("popovertargetaction", value) : self();
  }

  /**
   * Sets the <code>type</code> attribute.
   *
   * <p>The type of the button. Possible values are:
   *
   * <p>* <code>submit</code>: The button submits the form data to the server. This is the default if
   * the attribute is not specified, or if the attribute is dynamically changed to an empty or
   * invalid value. * <code>reset</code>: The button resets all the controls to their initial values.
   * * <code>button</code>: The button has no default behavior. It can have client-side scripts
   * associated with the element's events, which are triggered when the events occur.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/button">MDN:
   * type</a>
   */
  public ButtonTag withType(String value) {
    return attr("type", value);
  }

  /**
   * Applies {@link #withType(String)} only when {@code condition} is true.
   */
  public ButtonTag withCondType(boolean condition, String value) {
    return condition ? attr("type", value) : self();
  }

  /**
   * Sets the <code>value</code> attribute.
   *
   * <p>The initial value of the button. It defines the value associated with the button which is
   * submitted with the form data. This value is passed to the server in params when the form is
   * submitted.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/button">MDN:
   * value</a>
   */
  public ButtonTag withValue(String value) {
    return attr("value", value);
  }

  /**
   * Applies {@link #withValue(String)} only when {@code condition} is true.
   */
  public ButtonTag withCondValue(boolean condition, String value) {
    return condition ? attr("value", value) : self();
  }

  /**
   * Sets the <code>autocomplete</code> attribute.
   *
   * <p>The use of this attribute on a
   * [<code>&lt;button&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button
   * "The HTML &lt;button&gt; element represents a clickable button, which can be used in forms or
   * anywhere in a document that needs simple, standard button functionality.") is nonstandard and
   * Firefox-specific. By default, unlike other browsers, <a
   * href="https://stackoverflow.com/questions/5985839/bug-with-firefox-disabled-attribute-of-input-not-resetting-when-refreshing">Firefox
   * persists the dynamic disabled state</a> of a
   * [<code>&lt;button&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button
   * "The HTML &lt;button&gt; element represents a clickable button, which can be used in forms or
   * anywhere in a document that needs simple, standard button functionality.") across page loads.
   * Setting the value of this attribute to <code>off</code> (i.e. <code>autocomplete="off"</code>)
   * disables this feature. See [bug 654072](https://bugzilla.mozilla.org/show_bug.cgi?id=654072 "if
   * disabled state is changed with javascript, the normal state doesn't return after refreshing the
   * page").
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/button">MDN:
   * autocomplete</a>
   */
  public ButtonTag withAutocomplete(String value) {
    return attr("autocomplete", value);
  }

  /**
   * Applies {@link #withAutocomplete(String)} only when {@code condition} is true.
   */
  public ButtonTag withCondAutocomplete(boolean condition, String value) {
    return condition ? attr("autocomplete", value) : self();
  }
}
