// Generated by j2act-codegen. Do not edit; regenerate with:
//   ./mvnw -pl j2act-codegen compile exec:java
// Data: @vscode/web-custom-data (MIT), @mdn/browser-compat-data (CC0).
// API shape follows j2html (Apache-2.0). See NOTICE.
package j2act.html.tags;

import j2act.ContainerTag;

/**
 * The <code>&lt;option&gt;</code> element.
 *
 * <p>The option element represents an option in a select element or as part of a list of
 * suggestions in a datalist element.
 *
 * <p>Baseline: widely available since 2018-01-29.
 *
 * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/option">MDN:
 * &lt;option&gt;</a>
 */
public final class OptionTag extends ContainerTag<OptionTag> {

  public OptionTag() {
    super("option");
  }

  /**
   * Sets the <code>disabled</code> attribute.
   *
   * <p>If this Boolean attribute is set, this option is not checkable. Often browsers grey out such
   * control and it won't receive any browsing event, like mouse clicks or focus-related ones. If
   * this attribute is not set, the element can still be disabled if one of its ancestors is a
   * disabled
   * [<code>&lt;optgroup&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/optgroup
   * "The HTML &lt;optgroup&gt; element creates a grouping of options within a &lt;select&gt;
   * element.") element.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Attributes/disabled">MDN:
   * disabled</a>
   */
  public OptionTag isDisabled() {
    return attr("disabled");
  }

  /**
   * Applies {@link #isDisabled()} only when {@code condition} is true.
   */
  public OptionTag withCondDisabled(boolean condition) {
    return condition ? attr("disabled") : self();
  }

  /**
   * Sets the <code>label</code> attribute.
   *
   * <p>This attribute is text for the label indicating the meaning of the option. If the
   * <code>label</code> attribute isn't defined, its value is that of the element text content.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/option">MDN:
   * label</a>
   */
  public OptionTag withLabel(String value) {
    return attr("label", value);
  }

  /**
   * Applies {@link #withLabel(String)} only when {@code condition} is true.
   */
  public OptionTag withCondLabel(boolean condition, String value) {
    return condition ? attr("label", value) : self();
  }

  /**
   * Sets the <code>selected</code> attribute.
   *
   * <p>If present, this Boolean attribute indicates that the option is initially selected. If the
   * <code>&lt;option&gt;</code> element is the descendant of a
   * [<code>&lt;select&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/select
   * "The HTML &lt;select&gt; element represents a control that provides a menu of options") element
   * whose <a
   * href="https://developer.mozilla.org/en-US/docs/Web/HTML/Element/select#attr-multiple"><code>multiple</code></a>
   * attribute is not set, only one single <code>&lt;option&gt;</code> of this
   * [<code>&lt;select&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/select
   * "The HTML &lt;select&gt; element represents a control that provides a menu of options") element
   * may have the <code>selected</code> attribute.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/option">MDN:
   * selected</a>
   */
  public OptionTag isSelected() {
    return attr("selected");
  }

  /**
   * Applies {@link #isSelected()} only when {@code condition} is true.
   */
  public OptionTag withCondSelected(boolean condition) {
    return condition ? attr("selected") : self();
  }

  /**
   * Sets the <code>value</code> attribute.
   *
   * <p>The content of this attribute represents the value to be submitted with the form, should this
   * option be selected. If this attribute is omitted, the value is taken from the text content of
   * the option element.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/option">MDN:
   * value</a>
   */
  public OptionTag withValue(String value) {
    return attr("value", value);
  }

  /**
   * Applies {@link #withValue(String)} only when {@code condition} is true.
   */
  public OptionTag withCondValue(boolean condition, String value) {
    return condition ? attr("value", value) : self();
  }
}
