// Generated by j2act-codegen. Do not edit; regenerate with:
//   ./mvnw -pl j2act-codegen compile exec:java
// Data: @vscode/web-custom-data (MIT), @mdn/browser-compat-data (CC0).
// API shape follows j2html (Apache-2.0). See NOTICE.
package j2act.html.tags;

import j2act.EmptyTag;

/**
 * The <code>&lt;col&gt;</code> element.
 *
 * <p>If a col element has a parent and that is a colgroup element that itself has a parent that is
 * a table element, then the col element represents one or more columns in the column group
 * represented by that colgroup.
 *
 * <p>Baseline: widely available since 2018-01-29.
 *
 * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/col">MDN:
 * &lt;col&gt;</a>
 */
public final class ColTag extends EmptyTag<ColTag> {

  public ColTag() {
    super("col");
  }

  /**
   * Sets the <code>span</code> attribute.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/col">MDN: span</a>
   */
  public ColTag withSpan(String value) {
    return attr("span", value);
  }

  /**
   * Applies {@link #withSpan(String)} only when {@code condition} is true.
   */
  public ColTag withCondSpan(boolean condition, String value) {
    return condition ? attr("span", value) : self();
  }

  /**
   * Sets the <code>align</code> attribute.
   *
   * <p>This enumerated attribute specifies how horizontal alignment of each column cell content will
   * be handled. Possible values are:
   *
   * <p>* <code>left</code>, aligning the content to the left of the cell * <code>center</code>,
   * centering the content in the cell * <code>right</code>, aligning the content to the right of the
   * cell * <code>justify</code>, inserting spaces into the textual content so that the content is
   * justified in the cell * <code>char</code>, aligning the textual content on a special character
   * with a minimal offset, defined by the <a
   * href="https://developer.mozilla.org/en-US/docs/Web/HTML/Element/col#attr-char"><code>char</code></a>
   * and <a
   * href="https://developer.mozilla.org/en-US/docs/Web/HTML/Element/col#attr-charoff"><code>charoff</code></a>
   * attributes Unimplemented (see [bug 2212](https://bugzilla.mozilla.org/show_bug.cgi?id=2212
   * "character alignment not implemented (align=char, charoff=, text-align:&lt;string&gt;)")).
   *
   * <p>If this attribute is not set, its value is inherited from the <a
   * href="https://developer.mozilla.org/en-US/docs/Web/HTML/Element/colgroup#attr-align"><code>align</code></a>
   * of the
   * [<code>&lt;colgroup&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/colgroup
   * "The HTML &lt;colgroup&gt; element defines a group of columns within a table.") element this
   * <code>&lt;col&gt;</code> element belongs too. If there are none, the <code>left</code> value is
   * assumed.
   *
   * <p>**Note:** Do not use this attribute as it is obsolete (not supported) in the latest standard.
   *
   * <p>* To achieve the same effect as the <code>left</code>, <code>center</code>,
   * <code>right</code> or <code>justify</code> values: * Do not try to set the
   * [<code>text-align</code>](https://developer.mozilla.org/en-US/docs/Web/CSS/text-align "The
   * text-align CSS property sets the horizontal alignment of an inline or table-cell box. This means
   * it works like vertical-align but in the horizontal direction.") property on a selector giving a
   * [<code>&lt;col&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/col "The
   * HTML &lt;col&gt; element defines a column within a table and is used for defining common
   * semantics on all common cells. It is generally found within a &lt;colgroup&gt; element.")
   * element. Because
   * [<code>&lt;td&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/td "The HTML
   * &lt;td&gt; element defines a cell of a table that contains data. It participates in the table
   * model.") elements are not descendant of the
   * [<code>&lt;col&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/col "The
   * HTML &lt;col&gt; element defines a column within a table and is used for defining common
   * semantics on all common cells. It is generally found within a &lt;colgroup&gt; element.")
   * element, they won't inherit it. * If the table doesn't use a <a
   * href="https://developer.mozilla.org/en-US/docs/Web/HTML/Element/td#attr-colspan"><code>colspan</code></a>
   * attribute, use the <code>td:nth-child(an+b)</code> CSS selector. Set <code>a</code> to zero and
   * <code>b</code> to the position of the column in the table, e.g. <code>td:nth-child(2) {
   * text-align: right; }</code> to right-align the second column. * If the table does use a <a
   * href="https://developer.mozilla.org/en-US/docs/Web/HTML/Element/td#attr-colspan"><code>colspan</code></a>
   * attribute, the effect can be achieved by combining adequate CSS attribute selectors like
   * <code>[colspan=n]</code>, though this is not trivial. * To achieve the same effect as the
   * <code>char</code> value, in CSS3, you can use the value of the <a
   * href="https://developer.mozilla.org/en-US/docs/Web/HTML/Element/col#attr-char"><code>char</code></a>
   * as the value of the
   * [<code>text-align</code>](https://developer.mozilla.org/en-US/docs/Web/CSS/text-align "The
   * text-align CSS property sets the horizontal alignment of an inline or table-cell box. This means
   * it works like vertical-align but in the horizontal direction.") property Unimplemented.
   *
   * <p>Limited availability: not yet supported in all major browsers.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/col">MDN: align</a>
   * @deprecated The <code>align</code> attribute is marked deprecated in MDN browser-compat-data;
   * avoid in new markup.
   */
  @Deprecated
  public ColTag withAlign(String value) {
    return attr("align", value);
  }

  /**
   * Applies {@link #withAlign(String)} only when {@code condition} is true.
   */
  @Deprecated
  public ColTag withCondAlign(boolean condition, String value) {
    return condition ? attr("align", value) : self();
  }
}
