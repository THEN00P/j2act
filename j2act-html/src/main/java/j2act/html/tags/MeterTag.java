// Generated by j2act-codegen. Do not edit; regenerate with:
//   ./mvnw -pl j2act-codegen compile exec:java
// Data: @vscode/web-custom-data (MIT), @mdn/browser-compat-data (CC0).
// API shape follows j2html (Apache-2.0). See NOTICE.
package j2act.html.tags;

import j2act.ContainerTag;

/**
 * The <code>&lt;meter&gt;</code> element.
 *
 * <p>The meter element represents a scalar measurement within a known range, or a fractional
 * value; for example disk usage, the relevance of a query result, or the fraction of a voting
 * population to have selected a particular candidate.
 *
 * <p>Baseline: widely available since 2019-09-27.
 *
 * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/meter">MDN:
 * &lt;meter&gt;</a>
 */
public final class MeterTag extends ContainerTag<MeterTag> {

  public MeterTag() {
    super("meter");
  }

  /**
   * Sets the <code>value</code> attribute.
   *
   * <p>The current numeric value. This must be between the minimum and maximum values
   * (<code>min</code> attribute and <code>max</code> attribute) if they are specified. If
   * unspecified or malformed, the value is 0. If specified, but not within the range given by the
   * <code>min</code> attribute and <code>max</code> attribute, the value is equal to the nearest end
   * of the range.
   *
   * <p>**Usage note:** Unless the <code>value</code> attribute is between <code>0</code> and
   * <code>1</code> (inclusive), the <code>min</code> and <code>max</code> attributes should define
   * the range so that the <code>value</code> attribute's value is within it.
   *
   * <p>Baseline: widely available since 2019-09-27.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/meter">MDN:
   * value</a>
   */
  public MeterTag withValue(String value) {
    return attr("value", value);
  }

  /**
   * Applies {@link #withValue(String)} only when {@code condition} is true.
   */
  public MeterTag withCondValue(boolean condition, String value) {
    return condition ? attr("value", value) : self();
  }

  /**
   * Sets the <code>min</code> attribute.
   *
   * <p>The lower numeric bound of the measured range. This must be less than the maximum value
   * (<code>max</code> attribute), if specified. If unspecified, the minimum value is 0.
   *
   * <p>Baseline: widely available since 2019-09-27.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Attributes/min">MDN: min</a>
   */
  public MeterTag withMin(String value) {
    return attr("min", value);
  }

  /**
   * Applies {@link #withMin(String)} only when {@code condition} is true.
   */
  public MeterTag withCondMin(boolean condition, String value) {
    return condition ? attr("min", value) : self();
  }

  /**
   * Sets the <code>max</code> attribute.
   *
   * <p>The upper numeric bound of the measured range. This must be greater than the minimum value
   * (<code>min</code> attribute), if specified. If unspecified, the maximum value is 1.
   *
   * <p>Baseline: widely available since 2019-09-27.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Attributes/max">MDN: max</a>
   */
  public MeterTag withMax(String value) {
    return attr("max", value);
  }

  /**
   * Applies {@link #withMax(String)} only when {@code condition} is true.
   */
  public MeterTag withCondMax(boolean condition, String value) {
    return condition ? attr("max", value) : self();
  }

  /**
   * Sets the <code>low</code> attribute.
   *
   * <p>The upper numeric bound of the low end of the measured range. This must be greater than the
   * minimum value (<code>min</code> attribute), and it also must be less than the high value and
   * maximum value (<code>high</code> attribute and <code>max</code> attribute, respectively), if any
   * are specified. If unspecified, or if less than the minimum value, the <code>low</code> value is
   * equal to the minimum value.
   *
   * <p>Baseline: widely available since 2019-09-27.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/meter">MDN: low</a>
   */
  public MeterTag withLow(String value) {
    return attr("low", value);
  }

  /**
   * Applies {@link #withLow(String)} only when {@code condition} is true.
   */
  public MeterTag withCondLow(boolean condition, String value) {
    return condition ? attr("low", value) : self();
  }

  /**
   * Sets the <code>high</code> attribute.
   *
   * <p>The lower numeric bound of the high end of the measured range. This must be less than the
   * maximum value (<code>max</code> attribute), and it also must be greater than the low value and
   * minimum value (<code>low</code> attribute and **min** attribute, respectively), if any are
   * specified. If unspecified, or if greater than the maximum value, the <code>high</code> value is
   * equal to the maximum value.
   *
   * <p>Baseline: widely available since 2019-09-27.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/meter">MDN:
   * high</a>
   */
  public MeterTag withHigh(String value) {
    return attr("high", value);
  }

  /**
   * Applies {@link #withHigh(String)} only when {@code condition} is true.
   */
  public MeterTag withCondHigh(boolean condition, String value) {
    return condition ? attr("high", value) : self();
  }

  /**
   * Sets the <code>optimum</code> attribute.
   *
   * <p>This attribute indicates the optimal numeric value. It must be within the range (as defined
   * by the <code>min</code> attribute and <code>max</code> attribute). When used with the
   * <code>low</code> attribute and <code>high</code> attribute, it gives an indication where along
   * the range is considered preferable. For example, if it is between the <code>min</code> attribute
   * and the <code>low</code> attribute, then the lower range is considered preferred.
   *
   * <p>Baseline: widely available since 2019-09-27.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/meter">MDN:
   * optimum</a>
   */
  public MeterTag withOptimum(String value) {
    return attr("optimum", value);
  }

  /**
   * Applies {@link #withOptimum(String)} only when {@code condition} is true.
   */
  public MeterTag withCondOptimum(boolean condition, String value) {
    return condition ? attr("optimum", value) : self();
  }

  /**
   * Sets the <code>form</code> attribute.
   *
   * <p>This attribute associates the element with a <code>form</code> element that has ownership of
   * the <code>meter</code> element. For example, a <code>meter</code> might be displaying a range
   * corresponding to an <code>input</code> element of <code>type</code> _number_. This attribute is
   * only used if the <code>meter</code> element is being used as a form-associated element; even
   * then, it may be omitted if the element appears as a descendant of a <code>form</code> element.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/meter">MDN:
   * form</a>
   */
  public MeterTag withForm(String value) {
    return attr("form", value);
  }

  /**
   * Applies {@link #withForm(String)} only when {@code condition} is true.
   */
  public MeterTag withCondForm(boolean condition, String value) {
    return condition ? attr("form", value) : self();
  }
}
