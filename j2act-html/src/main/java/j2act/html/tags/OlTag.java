// Generated by j2act-codegen. Do not edit; regenerate with:
//   ./mvnw -pl j2act-codegen compile exec:java
// Data: @vscode/web-custom-data (MIT), @mdn/browser-compat-data (CC0).
// API shape follows j2html (Apache-2.0). See NOTICE.
package j2act.html.tags;

import j2act.ContainerTag;

/**
 * The <code>&lt;ol&gt;</code> element.
 *
 * <p>The ol element represents a list of items, where the items have been intentionally ordered,
 * such that changing the order would change the meaning of the document.
 *
 * <p>Baseline: widely available since 2018-01-29.
 *
 * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/ol">MDN:
 * &lt;ol&gt;</a>
 */
public final class OlTag extends ContainerTag<OlTag> {

  public OlTag() {
    super("ol");
  }

  /**
   * Sets the <code>reversed</code> attribute.
   *
   * <p>This Boolean attribute specifies that the items of the list are specified in reversed order.
   *
   * <p>Baseline: widely available since 2022-07-15.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/ol">MDN:
   * reversed</a>
   */
  public OlTag isReversed() {
    return attr("reversed");
  }

  /**
   * Applies {@link #isReversed()} when {@code condition} is true and removes the attribute
   * otherwise, so a form control stays controlled either way (ADR 0013).
   */
  public OlTag withCondReversed(boolean condition) {
    return condition ? attr("reversed") : attr("reversed", null);
  }

  /**
   * Sets the <code>start</code> attribute.
   *
   * <p>This integer attribute specifies the start value for numbering the individual list items.
   * Although the ordering type of list elements might be Roman numerals, such as XXXI, or letters,
   * the value of start is always represented as a number. To start numbering elements from the
   * letter "C", use <code>&lt;ol start="3"&gt;</code>.
   *
   * <p>**Note**: This attribute was deprecated in HTML4, but reintroduced in HTML5.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/ol">MDN: start</a>
   */
  public OlTag withStart(String value) {
    return attr("start", value);
  }

  /**
   * Applies {@link #withStart(String)} only when {@code condition} is true.
   */
  public OlTag withCondStart(boolean condition, String value) {
    return condition ? attr("start", value) : self();
  }

  /**
   * Sets the <code>type</code> attribute.
   *
   * <p>Indicates the numbering type:
   *
   * <p>* <code>'a'</code> indicates lowercase letters, * <code>'A'</code> indicates uppercase
   * letters, * <code>'i'</code> indicates lowercase Roman numerals, * <code>'I'</code> indicates
   * uppercase Roman numerals, * and <code>'1'</code> indicates numbers (default).
   *
   * <p>The type set is used for the entire list unless a different <a
   * href="https://developer.mozilla.org/en-US/docs/Web/HTML/Element/li#attr-type"><code>type</code></a>
   * attribute is used within an enclosed
   * [<code>&lt;li&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/li "The HTML
   * &lt;li&gt; element is used to represent an item in a list. It must be contained in a parent
   * element: an ordered list (&lt;ol&gt;), an unordered list (&lt;ul&gt;), or a menu (&lt;menu&gt;).
   * In menus and unordered lists, list items are usually displayed using bullet points. In ordered
   * lists, they are usually displayed with an ascending counter on the left, such as a number or
   * letter.") element.
   *
   * <p>**Note:** This attribute was deprecated in HTML4, but reintroduced in HTML5.
   *
   * <p>Unless the value of the list number matters (e.g. in legal or technical documents where items
   * are to be referenced by their number/letter), the CSS
   * [<code>list-style-type</code>](https://developer.mozilla.org/en-US/docs/Web/CSS/list-style-type
   * "The list-style-type CSS property sets the marker (such as a disc, character, or custom counter
   * style) of a list item element.") property should be used instead.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/ol">MDN: type</a>
   */
  public OlTag withType(String value) {
    return attr("type", value);
  }

  /**
   * Applies {@link #withType(String)} only when {@code condition} is true.
   */
  public OlTag withCondType(boolean condition, String value) {
    return condition ? attr("type", value) : self();
  }

  /**
   * Sets the <code>compact</code> attribute.
   *
   * <p>This Boolean attribute hints that the list should be rendered in a compact style. The
   * interpretation of this attribute depends on the user agent and it doesn't work in all browsers.
   *
   * <p>**Warning:** Do not use this attribute, as it has been deprecated: the
   * [<code>&lt;ol&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/ol "The HTML
   * &lt;ol&gt; element represents an ordered list of items, typically rendered as a numbered list.")
   * element should be styled using <a href="https://developer.mozilla.org/en-US/docs/CSS">CSS</a>.
   * To give an effect similar to the <code>compact</code> attribute, the <a
   * href="https://developer.mozilla.org/en-US/docs/CSS">CSS</a> property
   * [<code>line-height</code>](https://developer.mozilla.org/en-US/docs/Web/CSS/line-height "The
   * line-height CSS property sets the amount of space used for lines, such as in text. On
   * block-level elements, it specifies the minimum height of line boxes within the element. On
   * non-replaced inline elements, it specifies the height that is used to calculate line box
   * height.") can be used with a value of <code>80%</code>.
   *
   * <p>Limited availability: not yet supported in all major browsers.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/ol">MDN:
   * compact</a>
   * @deprecated The <code>compact</code> attribute is marked deprecated in MDN browser-compat-data;
   * avoid in new markup.
   */
  @Deprecated
  public OlTag withCompact(String value) {
    return attr("compact", value);
  }

  /**
   * Applies {@link #withCompact(String)} only when {@code condition} is true.
   */
  @Deprecated
  public OlTag withCondCompact(boolean condition, String value) {
    return condition ? attr("compact", value) : self();
  }
}
