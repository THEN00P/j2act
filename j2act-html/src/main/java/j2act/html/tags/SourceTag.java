// Generated by j2act-codegen. Do not edit; regenerate with:
//   ./mvnw -pl j2act-codegen compile exec:java
// Data: @vscode/web-custom-data (MIT), @mdn/browser-compat-data (CC0).
// API shape follows j2html (Apache-2.0). See NOTICE.
package j2act.html.tags;

import j2act.EmptyTag;

/**
 * The <code>&lt;source&gt;</code> element.
 *
 * <p>The source element allows authors to specify multiple alternative media resources for media
 * elements. It does not represent anything on its own.
 *
 * <p>Baseline: widely available since 2018-01-29.
 *
 * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/source">MDN:
 * &lt;source&gt;</a>
 */
public final class SourceTag extends EmptyTag<SourceTag> {

  public SourceTag() {
    super("source");
  }

  /**
   * Sets the <code>src</code> attribute.
   *
   * <p>Required for
   * [<code>&lt;audio&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/audio
   * "The HTML &lt;audio&gt; element is used to embed sound content in documents. It may contain one
   * or more audio sources, represented using the src attribute or the &lt;source&gt; element: the
   * browser will choose the most suitable one. It can also be the destination for streamed media,
   * using a MediaStream.") and
   * [<code>&lt;video&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/video
   * "The HTML Video element (&lt;video&gt;) embeds a media player which supports video playback into
   * the document."), address of the media resource. The value of this attribute is ignored when the
   * <code>&lt;source&gt;</code> element is placed inside a
   * [<code>&lt;picture&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/picture
   * "The HTML &lt;picture&gt; element contains zero or more &lt;source&gt; elements and one
   * &lt;img&gt; element to provide versions of an image for different display/device scenarios.")
   * element.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/source">MDN:
   * src</a>
   */
  public SourceTag withSrc(String value) {
    return attr("src", value);
  }

  /**
   * Applies {@link #withSrc(String)} only when {@code condition} is true.
   */
  public SourceTag withCondSrc(boolean condition, String value) {
    return condition ? attr("src", value) : self();
  }

  /**
   * Sets the <code>type</code> attribute.
   *
   * <p>The MIME-type of the resource, optionally with a <code>codecs</code> parameter. See <a
   * href="https://tools.ietf.org/html/rfc4281">RFC 4281</a> for information about how to specify
   * codecs.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/source">MDN:
   * type</a>
   */
  public SourceTag withType(String value) {
    return attr("type", value);
  }

  /**
   * Applies {@link #withType(String)} only when {@code condition} is true.
   */
  public SourceTag withCondType(boolean condition, String value) {
    return condition ? attr("type", value) : self();
  }

  /**
   * Sets the <code>sizes</code> attribute.
   *
   * <p>Is a list of source sizes that describes the final rendered width of the image represented by
   * the source. Each source size consists of a comma-separated list of media condition-length pairs.
   * This information is used by the browser to determine, before laying the page out, which image
   * defined in <a
   * href="https://developer.mozilla.org/en-US/docs/Web/HTML/Element/source#attr-srcset"><code>srcset</code></a>
   * to use. The <code>sizes</code> attribute has an effect only when the
   * [<code>&lt;source&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/source
   * "The HTML &lt;source&gt; element specifies multiple media resources for the &lt;picture&gt;, the
   * &lt;audio&gt; element, or the &lt;video&gt; element.") element is the direct child of a
   * [<code>&lt;picture&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/picture
   * "The HTML &lt;picture&gt; element contains zero or more &lt;source&gt; elements and one
   * &lt;img&gt; element to provide versions of an image for different display/device scenarios.")
   * element.
   *
   * <p>Baseline: widely available since 2018-09-21.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/source">MDN:
   * sizes</a>
   */
  public SourceTag withSizes(String value) {
    return attr("sizes", value);
  }

  /**
   * Applies {@link #withSizes(String)} only when {@code condition} is true.
   */
  public SourceTag withCondSizes(boolean condition, String value) {
    return condition ? attr("sizes", value) : self();
  }

  /**
   * Sets the <code>srcset</code> attribute.
   *
   * <p>A list of one or more strings separated by commas indicating a set of possible images
   * represented by the source for the browser to use. Each string is composed of:
   *
   * <p>1. one URL to an image, 2. a width descriptor, that is a positive integer directly followed
   * by <code>'w'</code>. The default value, if missing, is the infinity. 3. a pixel density
   * descriptor, that is a positive floating number directly followed by <code>'x'</code>. The
   * default value, if missing, is <code>1x</code>.
   *
   * <p>Each string in the list must have at least a width descriptor or a pixel density descriptor
   * to be valid. Among the list, there must be only one string containing the same tuple of width
   * descriptor and pixel density descriptor. The browser chooses the most adequate image to display
   * at a given point of time. The <code>srcset</code> attribute has an effect only when the
   * [<code>&lt;source&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/source
   * "The HTML &lt;source&gt; element specifies multiple media resources for the &lt;picture&gt;, the
   * &lt;audio&gt; element, or the &lt;video&gt; element.") element is the direct child of a
   * [<code>&lt;picture&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/picture
   * "The HTML &lt;picture&gt; element contains zero or more &lt;source&gt; elements and one
   * &lt;img&gt; element to provide versions of an image for different display/device scenarios.")
   * element.
   *
   * <p>Baseline: widely available since 2018-09-21.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/source">MDN:
   * srcset</a>
   */
  public SourceTag withSrcset(String value) {
    return attr("srcset", value);
  }

  /**
   * Applies {@link #withSrcset(String)} only when {@code condition} is true.
   */
  public SourceTag withCondSrcset(boolean condition, String value) {
    return condition ? attr("srcset", value) : self();
  }

  /**
   * Sets the <code>media</code> attribute.
   *
   * <p><a href="https://developer.mozilla.org/en-US/docs/CSS/Media_queries">Media query</a> of the
   * resource's intended media; this should be used only in a
   * [<code>&lt;picture&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/picture
   * "The HTML &lt;picture&gt; element contains zero or more &lt;source&gt; elements and one
   * &lt;img&gt; element to provide versions of an image for different display/device scenarios.")
   * element.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/source">MDN:
   * media</a>
   */
  public SourceTag withMedia(String value) {
    return attr("media", value);
  }

  /**
   * Applies {@link #withMedia(String)} only when {@code condition} is true.
   */
  public SourceTag withCondMedia(boolean condition, String value) {
    return condition ? attr("media", value) : self();
  }
}
