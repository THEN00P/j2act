// Generated by j2act-codegen. Do not edit; regenerate with:
//   ./mvnw -pl j2act-codegen compile exec:java
// Data: @vscode/web-custom-data (MIT), @mdn/browser-compat-data (CC0).
// API shape follows j2html (Apache-2.0). See NOTICE.
package j2act.html.tags;

import j2act.ContainerTag;

/**
 * The <code>&lt;textarea&gt;</code> element.
 *
 * <p>The textarea element represents a multiline plain text edit control for the element's raw
 * value. The contents of the control represent the control's default value.
 *
 * <p>Baseline: widely available since 2018-01-29.
 *
 * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/textarea">MDN:
 * &lt;textarea&gt;</a>
 */
public final class TextareaTag extends ContainerTag<TextareaTag> {

  public TextareaTag() {
    super("textarea");
  }

  /**
   * Sets the <code>autocomplete</code> attribute.
   *
   * <p>This attribute indicates whether the value of the control can be automatically completed by
   * the browser. Possible values are:
   *
   * <p>* <code>off</code>: The user must explicitly enter a value into this field for every use, or
   * the document provides its own auto-completion method; the browser does not automatically
   * complete the entry. * <code>on</code>: The browser can automatically complete the value based on
   * values that the user has entered during previous uses.
   *
   * <p>If the <code>autocomplete</code> attribute is not specified on a
   * <code>&lt;textarea&gt;</code> element, then the browser uses the <code>autocomplete</code>
   * attribute value of the <code>&lt;textarea&gt;</code> element's form owner. The form owner is
   * either the
   * [<code>&lt;form&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/form "The
   * HTML &lt;form&gt; element represents a document section that contains interactive controls for
   * submitting information to a web server.") element that this <code>&lt;textarea&gt;</code>
   * element is a descendant of or the form element whose <code>id</code> is specified by the
   * <code>form</code> attribute of the input element. For more information, see the <a
   * href="https://developer.mozilla.org/en-US/docs/Web/HTML/Element/form#attr-autocomplete"><code>autocomplete</code></a>
   * attribute in
   * [<code>&lt;form&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/form "The
   * HTML &lt;form&gt; element represents a document section that contains interactive controls for
   * submitting information to a web server.").
   *
   * <p>Baseline: widely available since 2022-07-15.
   *
   * @see <a
   * href="https://developer.mozilla.org/docs/Web/HTML/Reference/Attributes/autocomplete">MDN:
   * autocomplete</a>
   */
  public TextareaTag withAutocomplete(String value) {
    return attr("autocomplete", value);
  }

  /**
   * Applies {@link #withAutocomplete(String)} only when {@code condition} is true.
   */
  public TextareaTag withCondAutocomplete(boolean condition, String value) {
    return condition ? attr("autocomplete", value) : self();
  }

  /**
   * Sets the <code>cols</code> attribute.
   *
   * <p>The visible width of the text control, in average character widths. If it is specified, it
   * must be a positive integer. If it is not specified, the default value is <code>20</code>.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/textarea">MDN:
   * cols</a>
   */
  public TextareaTag withCols(String value) {
    return attr("cols", value);
  }

  /**
   * Applies {@link #withCols(String)} only when {@code condition} is true.
   */
  public TextareaTag withCondCols(boolean condition, String value) {
    return condition ? attr("cols", value) : self();
  }

  /**
   * Sets the <code>dirname</code> attribute.
   *
   * <p>Baseline: widely available since 2026-02-01.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Attributes/dirname">MDN:
   * dirname</a>
   */
  public TextareaTag withDirname(String value) {
    return attr("dirname", value);
  }

  /**
   * Applies {@link #withDirname(String)} only when {@code condition} is true.
   */
  public TextareaTag withCondDirname(boolean condition, String value) {
    return condition ? attr("dirname", value) : self();
  }

  /**
   * Sets the <code>disabled</code> attribute.
   *
   * <p>This Boolean attribute indicates that the user cannot interact with the control. If this
   * attribute is not specified, the control inherits its setting from the containing element, for
   * example
   * [<code>&lt;fieldset&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/fieldset
   * "The HTML &lt;fieldset&gt; element is used to group several controls as well as labels
   * (&lt;label&gt;) within a web form."); if there is no containing element when the
   * <code>disabled</code> attribute is set, the control is enabled.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Attributes/disabled">MDN:
   * disabled</a>
   */
  public TextareaTag isDisabled() {
    return attr("disabled");
  }

  /**
   * Applies {@link #isDisabled()} when {@code condition} is true and removes the attribute
   * otherwise, so a form control stays controlled either way (ADR 0013).
   */
  public TextareaTag withCondDisabled(boolean condition) {
    return condition ? attr("disabled") : attr("disabled", null);
  }

  /**
   * Sets the <code>form</code> attribute.
   *
   * <p>The form element that the <code>&lt;textarea&gt;</code> element is associated with (its "form
   * owner"). The value of the attribute must be the <code>id</code> of a form element in the same
   * document. If this attribute is not specified, the <code>&lt;textarea&gt;</code> element must be
   * a descendant of a form element. This attribute enables you to place
   * <code>&lt;textarea&gt;</code> elements anywhere within a document, not just as descendants of
   * form elements.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Attributes/form">MDN:
   * form</a>
   */
  public TextareaTag withForm(String value) {
    return attr("form", value);
  }

  /**
   * Applies {@link #withForm(String)} only when {@code condition} is true.
   */
  public TextareaTag withCondForm(boolean condition, String value) {
    return condition ? attr("form", value) : self();
  }

  /**
   * Sets the <code>maxlength</code> attribute.
   *
   * <p>The maximum number of characters (unicode code points) that the user can enter. If this value
   * isn't specified, the user can enter an unlimited number of characters.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Attributes/maxlength">MDN:
   * maxlength</a>
   */
  public TextareaTag withMaxlength(String value) {
    return attr("maxlength", value);
  }

  /**
   * Applies {@link #withMaxlength(String)} only when {@code condition} is true.
   */
  public TextareaTag withCondMaxlength(boolean condition, String value) {
    return condition ? attr("maxlength", value) : self();
  }

  /**
   * Sets the <code>minlength</code> attribute.
   *
   * <p>The minimum number of characters (unicode code points) required that the user should enter.
   *
   * <p>Baseline: widely available since 2020-10-30.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Attributes/minlength">MDN:
   * minlength</a>
   */
  public TextareaTag withMinlength(String value) {
    return attr("minlength", value);
  }

  /**
   * Applies {@link #withMinlength(String)} only when {@code condition} is true.
   */
  public TextareaTag withCondMinlength(boolean condition, String value) {
    return condition ? attr("minlength", value) : self();
  }

  /**
   * Sets the <code>name</code> attribute.
   *
   * <p>The name of the control.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/textarea">MDN:
   * name</a>
   */
  public TextareaTag withName(String value) {
    return attr("name", value);
  }

  /**
   * Applies {@link #withName(String)} only when {@code condition} is true.
   */
  public TextareaTag withCondName(boolean condition, String value) {
    return condition ? attr("name", value) : self();
  }

  /**
   * Sets the <code>placeholder</code> attribute.
   *
   * <p>A hint to the user of what can be entered in the control. Carriage returns or line-feeds
   * within the placeholder text must be treated as line breaks when rendering the hint.
   *
   * <p>**Note:** Placeholders should only be used to show an example of the type of data that should
   * be entered into a form; they are _not_ a substitute for a proper
   * [<code>&lt;label&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/label
   * "The HTML &lt;label&gt; element represents a caption for an item in a user interface.") element
   * tied to the input. See [Labels and
   * placeholders](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#Labels_and_placeholders
   * "The HTML &lt;input&gt; element is used to create interactive controls for web-based forms in
   * order to accept data from the user; a wide variety of types of input data and control widgets
   * are available, depending on the device and user agent.") in [&lt;input&gt;: The Input (Form
   * Input) element](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input "The HTML
   * &lt;input&gt; element is used to create interactive controls for web-based forms in order to
   * accept data from the user; a wide variety of types of input data and control widgets are
   * available, depending on the device and user agent.") for a full explanation.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Attributes/placeholder">MDN:
   * placeholder</a>
   */
  public TextareaTag withPlaceholder(String value) {
    return attr("placeholder", value);
  }

  /**
   * Applies {@link #withPlaceholder(String)} only when {@code condition} is true.
   */
  public TextareaTag withCondPlaceholder(boolean condition, String value) {
    return condition ? attr("placeholder", value) : self();
  }

  /**
   * Sets the <code>readonly</code> attribute.
   *
   * <p>This Boolean attribute indicates that the user cannot modify the value of the control. Unlike
   * the <code>disabled</code> attribute, the <code>readonly</code> attribute does not prevent the
   * user from clicking or selecting in the control. The value of a read-only control is still
   * submitted with the form.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Attributes/readonly">MDN:
   * readonly</a>
   */
  public TextareaTag isReadonly() {
    return attr("readonly");
  }

  /**
   * Applies {@link #isReadonly()} when {@code condition} is true and removes the attribute
   * otherwise, so a form control stays controlled either way (ADR 0013).
   */
  public TextareaTag withCondReadonly(boolean condition) {
    return condition ? attr("readonly") : attr("readonly", null);
  }

  /**
   * Sets the <code>required</code> attribute.
   *
   * <p>This attribute specifies that the user must fill in a value before submitting a form.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Attributes/required">MDN:
   * required</a>
   */
  public TextareaTag isRequired() {
    return attr("required");
  }

  /**
   * Applies {@link #isRequired()} when {@code condition} is true and removes the attribute
   * otherwise, so a form control stays controlled either way (ADR 0013).
   */
  public TextareaTag withCondRequired(boolean condition) {
    return condition ? attr("required") : attr("required", null);
  }

  /**
   * Sets the <code>rows</code> attribute.
   *
   * <p>The number of visible text lines for the control.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/textarea">MDN:
   * rows</a>
   */
  public TextareaTag withRows(String value) {
    return attr("rows", value);
  }

  /**
   * Applies {@link #withRows(String)} only when {@code condition} is true.
   */
  public TextareaTag withCondRows(boolean condition, String value) {
    return condition ? attr("rows", value) : self();
  }

  /**
   * Sets the <code>wrap</code> attribute.
   *
   * <p>Indicates how the control wraps text. Possible values are:
   *
   * <p>* <code>hard</code>: The browser automatically inserts line breaks (CR+LF) so that each line
   * has no more than the width of the control; the <code>cols</code> attribute must also be
   * specified for this to take effect. * <code>soft</code>: The browser ensures that all line breaks
   * in the value consist of a CR+LF pair, but does not insert any additional line breaks. *
   * <code>off</code> : Like <code>soft</code> but changes appearance to <code>white-space:
   * pre</code> so line segments exceeding <code>cols</code> are not wrapped and the
   * <code>&lt;textarea&gt;</code> becomes horizontally scrollable.
   *
   * <p>If this attribute is not specified, <code>soft</code> is its default value.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/textarea">MDN:
   * wrap</a>
   */
  public TextareaTag withWrap(String value) {
    return attr("wrap", value);
  }

  /**
   * Applies {@link #withWrap(String)} only when {@code condition} is true.
   */
  public TextareaTag withCondWrap(boolean condition, String value) {
    return condition ? attr("wrap", value) : self();
  }
}
