// Generated by j2act-codegen. Do not edit; regenerate with:
//   ./mvnw -pl j2act-codegen compile exec:java
// Data: @vscode/web-custom-data (MIT), @mdn/browser-compat-data (CC0).
// API shape follows j2html (Apache-2.0). See NOTICE.
package j2act.html.tags;

import j2act.ContainerTag;

/**
 * The <code>&lt;audio&gt;</code> element.
 *
 * <p>An audio element represents a sound or audio stream.
 *
 * <p>Baseline: widely available since 2018-01-29.
 *
 * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/audio">MDN:
 * &lt;audio&gt;</a>
 */
public final class AudioTag extends ContainerTag<AudioTag> {

  public AudioTag() {
    super("audio");
  }

  /**
   * Sets the <code>src</code> attribute.
   *
   * <p>The URL of the audio to embed. This is subject to <a
   * href="https://developer.mozilla.org/en-US/docs/HTTP_access_control">HTTP access controls</a>.
   * This is optional; you may instead use the
   * [<code>&lt;source&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/source
   * "The HTML &lt;source&gt; element specifies multiple media resources for the &lt;picture&gt;, the
   * &lt;audio&gt; element, or the &lt;video&gt; element.") element within the audio block to specify
   * the audio to embed.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/audio">MDN: src</a>
   */
  public AudioTag withSrc(String value) {
    return attr("src", value);
  }

  /**
   * Applies {@link #withSrc(String)} only when {@code condition} is true.
   */
  public AudioTag withCondSrc(boolean condition, String value) {
    return condition ? attr("src", value) : self();
  }

  /**
   * Sets the <code>crossorigin</code> attribute.
   *
   * <p>This enumerated attribute indicates whether to use CORS to fetch the related image. <a
   * href="https://developer.mozilla.org/en-US/docs/CORS_Enabled_Image">CORS-enabled resources</a>
   * can be reused in the
   * [<code>&lt;canvas&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/canvas
   * "Use the HTML &lt;canvas&gt; element with either the canvas scripting API or the WebGL API to
   * draw graphics and animations.") element without being _tainted_. The allowed values are:
   *
   * <p>anonymous
   *
   * <p>Sends a cross-origin request without a credential. In other words, it sends the
   * <code>Origin:</code> HTTP header without a cookie, X.509 certificate, or performing HTTP Basic
   * authentication. If the server does not give credentials to the origin site (by not setting the
   * <code>Access-Control-Allow-Origin:</code> HTTP header), the image will be _tainted_, and its
   * usage restricted.
   *
   * <p>use-credentials
   *
   * <p>Sends a cross-origin request with a credential. In other words, it sends the
   * <code>Origin:</code> HTTP header with a cookie, a certificate, or performing HTTP Basic
   * authentication. If the server does not give credentials to the origin site (through
   * <code>Access-Control-Allow-Credentials:</code> HTTP header), the image will be _tainted_ and its
   * usage restricted.
   *
   * <p>When not present, the resource is fetched without a CORS request (i.e. without sending the
   * <code>Origin:</code> HTTP header), preventing its non-tainted used in
   * [<code>&lt;canvas&gt;</code>](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/canvas
   * "Use the HTML &lt;canvas&gt; element with either the canvas scripting API or the WebGL API to
   * draw graphics and animations.") elements. If invalid, it is handled as if the enumerated keyword
   * **anonymous** was used. See <a
   * href="https://developer.mozilla.org/en-US/docs/HTML/CORS_settings_attributes">CORS settings
   * attributes</a> for additional information.
   *
   * <p>Baseline: widely available since 2023-01-28.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Attributes/crossorigin">MDN:
   * crossorigin</a>
   */
  public AudioTag withCrossorigin(String value) {
    return attr("crossorigin", value);
  }

  /**
   * Applies {@link #withCrossorigin(String)} only when {@code condition} is true.
   */
  public AudioTag withCondCrossorigin(boolean condition, String value) {
    return condition ? attr("crossorigin", value) : self();
  }

  /**
   * Sets the <code>preload</code> attribute.
   *
   * <p>This enumerated attribute is intended to provide a hint to the browser about what the author
   * thinks will lead to the best user experience. It may have one of the following values:
   *
   * <p>* <code>none</code>: Indicates that the audio should not be preloaded. *
   * <code>metadata</code>: Indicates that only audio metadata (e.g. length) is fetched. *
   * <code>auto</code>: Indicates that the whole audio file can be downloaded, even if the user is
   * not expected to use it. * _empty string_: A synonym of the <code>auto</code> value.
   *
   * <p>If not set, <code>preload</code>'s default value is browser-defined (i.e. each browser may
   * have its own default value). The spec advises it to be set to <code>metadata</code>.
   *
   * <p>**Usage notes:**
   *
   * <p>* The <code>autoplay</code> attribute has precedence over <code>preload</code>. If
   * <code>autoplay</code> is specified, the browser would obviously need to start downloading the
   * audio for playback. * The browser is not forced by the specification to follow the value of this
   * attribute; it is a mere hint.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/audio">MDN:
   * preload</a>
   */
  public AudioTag withPreload(String value) {
    return attr("preload", value);
  }

  /**
   * Applies {@link #withPreload(String)} only when {@code condition} is true.
   */
  public AudioTag withCondPreload(boolean condition, String value) {
    return condition ? attr("preload", value) : self();
  }

  /**
   * Sets the <code>autoplay</code> attribute.
   *
   * <p>A Boolean attribute: if specified, the audio will automatically begin playback as soon as it
   * can do so, without waiting for the entire audio file to finish downloading.
   *
   * <p>**Note**: Sites that automatically play audio (or videos with an audio track) can be an
   * unpleasant experience for users, so should be avoided when possible. If you must offer autoplay
   * functionality, you should make it opt-in (requiring a user to specifically enable it). However,
   * this can be useful when creating media elements whose source will be set at a later time, under
   * user control.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/audio">MDN:
   * autoplay</a>
   */
  public AudioTag isAutoplay() {
    return attr("autoplay");
  }

  /**
   * Applies {@link #isAutoplay()} only when {@code condition} is true.
   */
  public AudioTag withCondAutoplay(boolean condition) {
    return condition ? attr("autoplay") : self();
  }

  /**
   * Sets the <code>mediagroup</code> attribute.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/audio">MDN:
   * mediagroup</a>
   */
  public AudioTag withMediagroup(String value) {
    return attr("mediagroup", value);
  }

  /**
   * Applies {@link #withMediagroup(String)} only when {@code condition} is true.
   */
  public AudioTag withCondMediagroup(boolean condition, String value) {
    return condition ? attr("mediagroup", value) : self();
  }

  /**
   * Sets the <code>loop</code> attribute.
   *
   * <p>A Boolean attribute: if specified, the audio player will automatically seek back to the
   * start upon reaching the end of the audio.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/audio">MDN:
   * loop</a>
   */
  public AudioTag isLoop() {
    return attr("loop");
  }

  /**
   * Applies {@link #isLoop()} only when {@code condition} is true.
   */
  public AudioTag withCondLoop(boolean condition) {
    return condition ? attr("loop") : self();
  }

  /**
   * Sets the <code>muted</code> attribute.
   *
   * <p>A Boolean attribute that indicates whether the audio will be initially silenced. Its default
   * value is <code>false</code>.
   *
   * <p>Baseline: widely available since ≤2021-04-02.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/audio">MDN:
   * muted</a>
   */
  public AudioTag isMuted() {
    return attr("muted");
  }

  /**
   * Applies {@link #isMuted()} only when {@code condition} is true.
   */
  public AudioTag withCondMuted(boolean condition) {
    return condition ? attr("muted") : self();
  }

  /**
   * Sets the <code>controls</code> attribute.
   *
   * <p>If this attribute is present, the browser will offer controls to allow the user to control
   * audio playback, including volume, seeking, and pause/resume playback.
   *
   * <p>Baseline: widely available since 2018-01-29.
   *
   * @see <a href="https://developer.mozilla.org/docs/Web/HTML/Reference/Elements/audio">MDN:
   * controls</a>
   */
  public AudioTag isControls() {
    return attr("controls");
  }

  /**
   * Applies {@link #isControls()} only when {@code condition} is true.
   */
  public AudioTag withCondControls(boolean condition) {
    return condition ? attr("controls") : self();
  }
}
